# CLAUDE.md

## Ghi chú phạm vi áp dụng
Tài liệu này gồm hai phần:
- **Phần I – Quy tắc chung**: áp dụng cho **tất cả các dự án phát triển phần mềm sau này**, không giới hạn ở dự án hiện tại. Khi bắt đầu một dự án mới, hãy sao chép nguyên Phần I làm điểm khởi đầu cho `CLAUDE.md` của dự án đó, sau đó viết thêm phần thông tin riêng của dự án theo đúng cấu trúc như Phần II bên dưới.
- **Phần II – Thông tin dự án hiện tại**: đặc thù cho dự án "Máy kiểm tra End-Of-Line 705/715" (khách hàng TTI, nhà cung cấp ALPHA VINA).

Khi Phần I được rút kinh nghiệm/cập nhật từ một dự án bất kỳ (kể cả không phải dự án này), nên đồng bộ lại thay đổi đó sang các dự án khác đang áp dụng bộ quy tắc này nếu phù hợp.

---

# PHẦN I — QUY TẮC CHUNG (áp dụng cho mọi dự án)

## 0. Quy tắc quy trình khi nhận task mới
- **Trước khi bắt đầu bất kỳ task nào, nếu có điểm chưa rõ ràng (yêu cầu mơ hồ, nhiều cách hiểu, thiếu thông tin cần thiết để làm đúng ngay lần đầu), phải xác nhận lại với người dùng NGAY LẬP TỨC trước khi code** — không tự đoán rồi làm, không vừa làm vừa đoán. Áp dụng cho mọi task sau này, không riêng dự án này (rule chung, đặt ở Phần I). Nếu yêu cầu đã đủ rõ, không cần hỏi lại, cứ làm thẳng.
- Với các task dạng sửa lỗi/đồng bộ dữ liệu giữa nhiều màn hình: sau khi sửa, phải **chạy test + verify UI thật theo vòng lặp** (sửa → build → test → verify → nếu còn sai thì lặp lại) cho tới khi xác nhận đạt yêu cầu, không dừng ở "có vẻ đã sửa đúng" dựa trên đọc code suông.

## 1. Nguyên tắc tài liệu hóa
- Mỗi dự án phải có một file `CLAUDE.md` ở thư mục gốc, theo đúng cấu trúc hai phần như tài liệu này: Quy tắc chung (kế thừa) + Thông tin riêng của dự án.
- Viết tài liệu bằng tiếng Việt có dấu; định danh trong code (tên biến/hàm/lớp) vẫn giữ tiếng Anh theo chuẩn ngành.
- Không tạo thêm file tài liệu/kế hoạch/ghi chú phân tích ngoài `CLAUDE.md` trừ khi được yêu cầu rõ ràng.
- **CLAUDE.md chỉ chứa quy tắc chung + trạng thái/đặc tả HIỆN HÀNH của dự án** — không chứa nhật ký lịch sử từng phiên làm việc (tài liệu này được nạp toàn bộ vào context mỗi phiên, phình to sẽ làm chậm/tốn ngữ cảnh mọi lúc, kể cả khi không liên quan). Khi kết thúc 1 vòng việc cần ghi "Tóm tắt phiên làm việc" (bối cảnh, quyết định + lý do, bẫy kỹ thuật), **append vào `docs/session-log/history.md`** (hoặc file lịch sử tương ứng của dự án đó), không ghi vào CLAUDE.md. Quy tắc này rút ra sau khi CLAUDE.md của dự án HV356 phình tới 506 dòng (quá nửa là lịch sử 6 phiên) — áp dụng cho mọi dự án mới ngay từ đầu, không đợi phình to mới tách.
- **File CSV trong dự án (seed data hay export runtime) phải lưu ở định dạng UTF-8 có BOM** ("CSV UTF-8" khi Excel Save As, không phải "CSV (Comma delimited)" thường dùng bảng mã hệ thống) — bắt buộc khi dự án có nội dung tiếng Việt có dấu trong CSV. Lỗi mất dấu do lưu sai encoding đã xảy ra thật, tái diễn nhiều lần trong dự án HV356 (`io-map-seed.csv`).

## 2. Công nghệ nền tảng mặc định (ứng dụng desktop công nghiệp)
- IDE: bản Visual Studio Community mới nhất đã cài trên máy tại thời điểm bắt đầu dự án (hiện tại: VS 2026, v18.7).
- Runtime: luôn chọn phiên bản .NET LTS mới nhất đã cài/khả dụng (hiện tại: .NET 10) — không dùng .NET Framework cho dự án mới, không chọn bản .NET sắp hết hạn hỗ trợ.
- UI: WPF + MVVM nghiêm ngặt (CommunityToolkit.Mvvm) cho ứng dụng desktop Windows nhiều màn hình/dữ liệu động. Không viết logic nghiệp vụ trong code-behind.
- DI/Hosting: `Microsoft.Extensions.Hosting` + `Microsoft.Extensions.DependencyInjection` làm composition root; logging có cấu trúc qua Serilog/`Microsoft.Extensions.Logging`, tách biệt hoàn toàn khỏi khung log hiển thị trên UI.
- Lưu trữ cấu hình: JSON cho file cấu hình cần con người đọc/sửa tay (kết nối, bản đồ tín hiệu, thông số...); SQLite cho dữ liệu cần truy vấn hoặc có khối lượng lớn (audit log, lịch sử kết quả...).

## 3. Kiến trúc & tách lớp
- Tách solution thành nhiều project theo trách nhiệm rõ ràng: tầng giao diện (App/UI), tầng domain thuần (Core — không phụ thuộc framework/IO), tầng giao tiếp phần cứng/bên ngoài (Communication), tầng cấu hình (Configuration), tầng dữ liệu (Data), tầng bảo mật (Security).
- **Không hardcode danh sách/bảng dữ liệu trong UI**: bất cứ thứ gì về bản chất là một danh sách (bước kiểm tra, điểm I/O, tham số cấu hình, người dùng...) phải được bind từ một collection, không viết cứng từng dòng trong XAML.
- **Trừu tượng hóa giao tiếp phần cứng/giao thức**: khi dự án cần giao tiếp với thiết bị ngoài (PLC, cảm biến, máy đọc mã vạch...) qua một giao thức cụ thể, luôn định nghĩa một interface trung lập (ví dụ `IPlcCommunicationDriver`) và cài đặt giao thức hiện tại phía sau interface đó. ViewModel/View không được phụ thuộc trực tiếp vào kiểu dữ liệu của giao thức cụ thể (Modbus, MC Protocol, SerialPort...). Mục tiêu: đổi hoặc thêm giao thức sau này chỉ cần viết driver mới, không sửa tầng trên.
- Mọi thao tác vào/ra phần cứng phải là `async`, chạy trên service nền riêng, không bao giờ block UI thread (cấm dùng `.Result`/`.Wait()`). Luôn có timeout + retry giới hạn cho mọi request tới thiết bị ngoài.
- Luôn có một chỉ báo trạng thái kết nối (Connected/Connecting/Error...) hiển thị cố định trên UI, không chỉ ghi lỗi âm thầm vào log.

## 4. Bảo mật & phân quyền
- Không dùng một mật khẩu phẳng cho mọi hành động nhạy cảm. Định nghĩa vai trò (role) rõ ràng theo mức độ rủi ro của hành động (ví dụ: Operator → Supervisor → Admin), quản lý qua enum + bảng quyền, không rải rác các điều kiện kiểu `if (password == "...")`.
- Mật khẩu luôn lưu dạng băm + salt (PBKDF2/BCrypt), không bao giờ lưu plaintext trong config hoặc log. Ô nhập mật khẩu trên UI luôn phải che ký tự.
- Mọi hành động có rủi ro chất lượng/an toàn (đổi thông số giới hạn kiểm tra, gửi lệnh điều khiển thủ công, bật tính năng bỏ qua lỗi/cảnh báo...) bắt buộc phải ghi audit log: ai, làm gì, giá trị cũ → mới, thời điểm nào. Đây là yêu cầu bắt buộc, không phải tùy chọn.
- Các tính năng có khả năng "qua mặt" kiểm tra chất lượng (ví dụ bỏ qua lỗi/NG) phải yêu cầu quyền cao hơn thao tác chỉnh sửa thông số thông thường, và không được âm thầm duy trì hiệu lực giữa các phiên làm việc mà không có xác nhận lại.

## 5. Quy ước viết code
- Bật nullable reference types; toàn bộ I/O dùng `async`/`await`, tên hàm async kết thúc bằng `Async`.
- MVVM nghiêm ngặt: View chỉ chứa mối quan tâm thuần giao diện; toàn bộ trạng thái/logic nằm ở ViewModel; ViewModel chỉ phụ thuộc vào interface ở tầng Core, không phụ thuộc trực tiếp vào implementation cụ thể (inject qua DI).
- Không thêm tính năng, refactor, hay trừu tượng hóa vượt quá yêu cầu thực tế của tác vụ đang làm. Ba dòng code giống nhau vẫn tốt hơn một abstraction làm sớm không cần thiết.
- Comment chỉ khi lý do (WHY) không hiển nhiên — quirk phần cứng/giao thức, workaround cho một lỗi cụ thể, ràng buộc ẩn. Không comment để mô tả lại việc code đang làm (WHAT).
- Chuỗi hiển thị trên UI nên tập trung ở resource file (`.resx`) thay vì literal rải rác trong code, để dễ sửa lỗi layout/dịch thuật sau này mà không phải sửa nhiều nơi.
- **Bẫy WPF hay gặp**: khi đặt nhiều khối có chiều cao khác nhau cạnh nhau trong `WrapPanel`/`StackPanel` ngang, các phần tử con có `VerticalAlignment` mặc định là `Stretch` sẽ tự giãn theo chiều cao của phần tử cao nhất trong cùng hàng — nếu một khối (vd. card thông tin nhiều dòng) cao hơn hẳn các khối còn lại (TextBox/Button đơn dòng), các khối kia sẽ bị kéo giãn thành ô trống to bất thường. Luôn đặt `VerticalAlignment="Top"` (hoặc `Center`) tường minh cho từng khối con khi các anh em có chiều cao chênh lệch lớn.

## 6. Kiểm thử
- Unit test cho mọi logic thuần túy (parse dữ liệu, so sánh giới hạn, encode/decode khung giao tiếp...) — không cần phần cứng thật.
- Với phần phụ thuộc phần cứng thật (PLC, cổng COM...), ghi chú rõ trong PR/commit rằng thay đổi cần được kiểm tra trên bàn thử nghiệm thực tế trước khi merge, vì không thể tự động hóa hoàn toàn bằng unit test.

## 7. Kiểm thử giao diện (UI) và biên bản đối chiếu
- Sau mỗi lần sửa giao diện theo yêu cầu, phải **chạy thử ứng dụng thật** (`dotnet run`) và chụp ảnh xác minh trước khi báo cáo hoàn thành — không suy đoán kết quả từ code.
- **Không bao giờ chụp toàn màn hình (full-screen capture qua `Screen.Bounds` + `CopyFromScreen`)** để lấy ảnh cửa sổ ứng dụng khi tự động hóa kiểm thử trên Windows. Trên máy có DPI scaling khác 100%, tiến trình gọi lệnh (vd PowerShell) thường không nhận biết DPI, khiến toạ độ/kích thước lấy được bị lệch tỉ lệ — hậu quả là ảnh chụp có thể lấy nhầm nội dung màn hình thật của người dùng (kể cả cửa sổ khác đang mở, có thể chứa thông tin nhạy cảm) thay vì cửa sổ ứng dụng đang test. Đây là rủi ro rò rỉ dữ liệu riêng tư đã từng xảy ra thật.
- Cách an toàn: gọi `SetProcessDPIAware()` rồi dùng `PrintWindow` (user32) chụp trực tiếp handle của cửa sổ ứng dụng cần kiểm tra — không phụ thuộc toạ độ màn hình, không thể vô tình chụp cửa sổ khác.
- Khi một vòng chỉnh sửa UI theo yêu cầu người dùng hoàn tất, nếu được yêu cầu tạo biên bản đối chiếu: lưu ảnh chụp (trước/sau, có thể kèm cả bản gốc nếu là dự án rebuild) và một file báo cáo (html/markdown) vào thư mục `Test/Report/` ở gốc dự án — tách biệt khỏi mã nguồn, dễ dọn dẹp, không lẫn vào `/docs`.
- Với yêu cầu UI thiếu ảnh/tài liệu tham chiếu cụ thể (vd "hiển thị dạng cột giống ảnh tôi cung cấp" nhưng không có ảnh đúng nội dung đó), triển khai theo cách hiểu hợp lý nhất và **ghi rõ đây là giả định cần xác nhận** trong báo cáo/tóm tắt — không dừng lại chờ xác nhận nếu việc đó không chặn toàn bộ tiến độ, vì các chi tiết UI luôn có thể chỉnh sửa lại theo phản hồi.

---

# PHẦN II — DỰ ÁN HIỆN TẠI: MÁY KIỂM TRA END-OF-LINE 705/715

## 1. Tổng quan dự án
- Sản phẩm: phần mềm điều khiển máy kiểm tra tự động cuối chuyền (EOL – End Of Line), hiện đang triển khai với tên "705/715 EOL machine" cho khách hàng TTI (do ALPHAVINA sản xuất;). Logo thật của cả hai bên đã được cung cấp (`docs/logo/Logo1.png` = TTI, `Logo2.png` = ALPHAVINA). **Hiện chỉ dùng `Logo1.png` (TTI)** trên UT — cả 2 vị trí `Logo2.png` (ALPHAVINA) ở header + khung phải tab Main đã bị gỡ khỏi XAML theo yêu cầu người dùng (còn file trong `Assets/`, chỉ không tham chiếu). Tiêu đề header + tên Model mặc định giờ đọc từ `src/EolTester.Configuration/SeedData/spec-Default.csv` (không hardcode trong resx/code — xem mục 9c), nên đổi tên sản phẩm sau này chỉ cần sửa CSV rồi build lại.
- Chức năng: kết nối với PLC qua RS485 (hiện tại là Modbus RTU) để chạy kiểm tra chức năng tự động (và giám sát/thủ công) trên sản phẩm đã lắp ráp (điện áp, dòng điện, lực hút chân không, LED...), so sánh kết quả với giới hạn có thể cấu hình để quyết định đạt/lỗi, và ghi nhận truy xuất nguồn gốc qua quét mã vạch/QR + số serial.
- Đây là dự án xây dựng lại một phần mềm desktop Windows đã có sẵn (xem ảnh tham khảo bên dưới). Cần giữ nguyên luồng thao tác/bố cục màn hình để người vận hành không phải học lại cách dùng, nhưng phải khắc phục các vấn đề cấu trúc của bản cũ (bảng dữ liệu hardcode, địa chỉ I/O hardcode, không có lớp trừu tượng giao thức, không có audit log, phân quyền yếu).
- Phần mềm phải tiếp tục hỗ trợ nhiều model/job sản phẩm khác nhau (xem các trường "Nhập Tên Model / Nhập Tên Job"), không chỉ dành riêng cho model 705/715.

## 2. Tài liệu tham khảo (ảnh giao diện bản cũ)
- `Automatic _Main.png` — Trang Tự động (tab Main/Auto)
- `Monitor page.png` — Giám sát/Thủ công (tab Monitor & điều khiển thủ công)
- `Setup page.png` — Cài đặt/Giới thiệu (tab Setup, hiện đang trống/chưa làm ở bản cũ)
- `Setting Page.png` — Set spec. (tab cấu hình thông số kiểm tra, có mật khẩu bảo vệ)

Khi triển khai từng màn hình tương ứng, cần đối chiếu lại các ảnh này — chúng thể hiện yêu cầu về trường dữ liệu/hành vi, không chỉ là kiểu dáng hiển thị.

## 3. Phân tích hệ thống bản cũ

### Điểm mạnh cần giữ lại
- Cấu trúc 4 tab rõ ràng, đúng luồng thao tác vận hành: Chạy tự động → Giám sát/Thủ công → Setup → Cấu hình thông số.
- Phần đầu trang (tab, đăng nhập/đăng xuất, mật khẩu) và phần cuối trang (thông tin Lot, cycle time, quét mã vạch, Start/Reset, xác nhận NG) hiển thị xuyên suốt mọi tab — người vận hành không bị mất ngữ cảnh khi chuyển tab. Đây phải là một khung ứng dụng (app shell) dùng chung duy nhất, không lặp lại ở từng trang.
- Thông số giới hạn (spec) ở tab Set spec. được phản chiếu dạng chỉ-đọc sang tab Main — tách biệt tốt giữa "cấu hình" (có quyền) và "quan sát" (vận hành) trên cùng một dữ liệu.
- Điều khiển I/O thủ công (tab Monitor) được khóa sau mật khẩu/chế độ Thủ công — hướng đi đúng, cần được thực thi chặt chẽ hơn nữa.
- Luồng mã vạch/số serial (mã SCAN OK gần nhất, số serial tiếp theo, tùy chọn bắt buộc đúng thứ tự tem) — nền tảng truy xuất nguồn gốc tốt, cần giữ lại.
- Cấu hình vị trí ký tự bắt đầu/kết thúc để tách số serial từ chuỗi mã vạch dài hơn — linh hoạt, giữ lại như một quy tắc có thể cấu hình.

### Điểm yếu / lỗi không được lặp lại
1. **Chữ bị cắt** — tiêu đề cửa sổ và tiêu đề trang bị cắt rõ ràng ("...HV3!", "...HV35"). Bố cục cố định theo pixel, không tương thích DPI. Cần dùng layout co giãn (WPF Grid/DockPanel, `TextTrimming`/tooltip), không dùng chiều rộng cố định.
2. **Hardcode bảng bước kiểm tra và bảng I/O** — bảng thông số (bước 1.1–3.3) và bảng I/O ở tab Monitor (X0–X27, Y0–Y27) có dấu hiệu là các dòng UI cố định với ô placeholder "..." thay vì bind từ danh sách. Việc thêm/bớt một bước kiểm tra hay một điểm I/O hiện có thể phải build lại phần mềm. Bắt buộc phải chuyển thành data-driven hoàn toàn.
3. **Không có chỉ báo trạng thái kết nối PLC** — cả 4 màn hình đều không hiển thị việc kết nối RS485/PLC đang OK, chập chờn hay lỗi; chỉ có một ô lịch sử thông báo/lỗi chung chung. Bắt buộc phải có chỉ báo tình trạng kết nối hiển thị cố định.
4. **Giá trị 0 gây mơ hồ** — các dòng thông số chưa cấu hình mặc định là "0" cho cả hai giới hạn, không phân biệt được với một giới hạn kỹ thuật thật sự bằng 0. Cần hiển thị rõ trạng thái "chưa cấu hình".
5. **Một ô mật khẩu duy nhất, phẳng** — cùng một ô "MẬT KHẨU" cho cả đăng nhập thường lẫn một "mật khẩu TEST" khác được ngụ ý riêng biệt, không thấy phân quyền rõ ràng. Cần chuyển thành hệ thống phân quyền theo vai trò chính thức.
6. **"BỎ QUA LỖI" (bỏ qua phát hiện NG)** — rủi ro chất lượng thực sự nếu một sản phẩm lỗi có thể lọt qua mà không bị phát hiện; bản cũ không có audit trail nào cho tính năng này.
7. **Nhãn I/O và thông số gắn cứng 1:1 với địa chỉ PLC** (ví dụ "X4 = NÚT START TRÁI") — muốn đổi địa chỉ PLC hoặc đổi tên tín hiệu phải sửa code. Cần đưa ra file cấu hình bên ngoài.
8. **Tab Setup đang trống** — các tham số kết nối (giao thức, cổng COM, baud rate, parity, địa chỉ trạm...) chưa được làm ở bản cũ; đây là cơ hội để thiết kế đúng ngay từ đầu theo hướng độc lập giao thức.
9. **Không có lịch sử thay đổi thông số spec** — các giá trị quan trọng về chất lượng (giới hạn điện áp/dòng điện/áp suất) có thể bị đổi mà không có bản ghi ai đổi/khi nào/giá trị cũ → mới.

### Cơ hội cải tiến (ngoài việc chỉ tương đương bản cũ)
- Huy hiệu trạng thái kết nối cố định (Connected / Đang kết nối lại / Lỗi + thông báo lỗi gần nhất) trong khung ứng dụng dùng chung.
- Audit log dạng chỉ-ghi-thêm (append-only) cho: đăng nhập/đăng xuất, thay đổi thông số/giới hạn, lệnh PLC thủ công ở chế độ Thủ công.
- Bảng thông số + bản đồ I/O data-driven (lưu dạng JSON) để tab Setup/Setting có thể thêm/sửa dòng mà không cần build lại phần mềm.
- Lớp giao tiếp độc lập giao thức để RS485/Modbus RTU hiện tại có thể mở rộng sang MC Protocol, SLMP, CC-Link, hoặc EtherNet/IP sau này mà không phải sửa ViewModel hay View (xem mục 6).
- Cấu hình có versioning + export/import theo từng model sản phẩm, để khi đổi "Model" ở thông tin Lot có thể nạp cả bộ profile spec/I-O thay vì nhập tay lại từ đầu.

## 4. Công nghệ đã chốt cho dự án này
- **IDE**: Visual Studio Community 2026 (v18.7, đã cài sẵn trên máy).
- **Runtime**: .NET 10 (LTS, đã cài sẵn — SDK 10.0.301). Không dùng .NET 8 hay .NET Framework — .NET 10 có thời gian hỗ trợ dài hơn (đến khoảng tháng 11/2028), quan trọng với phần mềm thiết bị công nghiệp có vòng đời sử dụng dài.
- **UI**: WPF, MVVM nghiêm ngặt (CommunityToolkit.Mvvm cho `ObservableObject` / `RelayCommand` / messaging). Không viết logic nghiệp vụ trong code-behind — View chỉ bind tới ViewModel. Chạy dạng cửa sổ thường (có minimize/close), không phải kiosk toàn màn hình.
- **Đa ngôn ngữ**: giao diện hỗ trợ **Việt + Anh**, chuyển đổi ngay lúc chạy (không cần khởi động lại), toàn bộ chuỗi hiển thị đưa vào `.resx`. **Đã cài đặt thật (không còn là mục tiêu)** — xem mục 9b để biết kiến trúc cụ thể (`Translation`, `TrExtension`, `ILanguageService`).
- **DI/Hosting**: `Microsoft.Extensions.Hosting` + `Microsoft.Extensions.DependencyInjection` làm composition root; `Microsoft.Extensions.Logging` + Serilog (ghi ra file) cho log có cấu trúc, tách biệt khỏi ô lịch sử thông báo hiển thị trên UI.
- **Giao tiếp PLC (hiện tại)**: Modbus RTU qua RS485 (`System.IO.Ports.SerialPort` + thư viện Modbus RTU, ví dụ NModbus), được bọc sau một lớp trừu tượng độc lập giao thức — xem mục 6. Phần mềm không được phép gắn chặt vào riêng Modbus RTU. **`ModbusRtuDriver` (real, mở cổng COM thật) là driver DUY NHẤT** khi Role=Master — đã bỏ hẳn `MockModbusDriver`/driver giả lập (từng dùng để dev/demo không cần phần cứng, nhưng bị đánh giá là rủi ro vận hành thật: nếu ai đó vô tình để chế độ giả lập trên máy sản xuất thật, app sẽ báo "Đã kết nối" và hiện dữ liệu giả mà không có PLC thật nào phía sau). Không còn khái niệm build Debug/Release khác hành vi kết nối — mọi build đều bắt buộc kết nối PLC thật để hiển thị "Đã kết nối"/dữ liệu đo lường. `PlcPollingService.GenerateDemoMeasurement` (dữ liệu đo lường giả cho bước Set Spec chưa gán Address) vẫn còn, nhưng **chỉ bật ở build Debug** (`#if DEBUG`) và không thể bật lại qua UI — Release luôn trả về 0 cố định, không phải rủi ro vận hành runtime-switchable như Mock driver cũ.
- **Máy quét mã vạch/QR**: kết nối kiểu USB "bàn phím ảo" (keyboard wedge) — dữ liệu quét được gõ thẳng vào control đang có keyboard focus như bàn phím thật, không cần driver/cổng COM riêng cho scanner. `KeyboardWedgeScanCapture` hook `PreviewTextInput`/`PreviewKeyDown` ở cấp Window **nhưng CHỈ can thiệp khi ô "Mã Scan quét được" (`ScanInputBox`) đang có keyboard focus** — trước đây bắt toàn cục không phụ thuộc focus, gây bug thật: ở SCAN MODE khi máy chưa sẵn sàng thì nuốt MỌI ký tự gõ vào bất kỳ ô nào (login/Job/Setup...), không gõ được gì. Đánh đổi đã thống nhất: quét khi focus ở ô khác có thể để lọt ký tự vào ô đó (FixedLength có thể thừa 1 ký tự → quét lại). Hỗ trợ 3 kiểu nhận diện điểm bắt đầu/kết thúc — xem mục 7 Tab 1 "Scan Barcode".
- **Lưu trữ**: file JSON cho cấu hình spec / bản đồ I/O / kết nối (dễ đọc/sửa tay khi bảo trì tại hiện trường); SQLite (`Microsoft.Data.Sqlite`) cho audit log và lịch sử kết quả/NG khi cần truy vấn nhiều.
- **Xuất dữ liệu**: giai đoạn hiện tại chỉ cần **xuất file CSV** kết quả kiểm tra (cấu trúc cột cụ thể sẽ chốt sau). Tích hợp trực tiếp với MES/ERP thực tế của nhà máy là **tính năng option, để dành cho giai đoạn nâng cấp sau** — chỉ cần thiết kế tầng `Data`/export theo interface (`IResultExporter` hay tương tự) để sau này thêm một implementation mới (gọi API MES) mà không phải sửa lại logic ghi kết quả, không code phần tích hợp MES cụ thể ngay bây giờ.

### Lệnh thường dùng (build / test / run / publish)
Không có file `.sln` — dùng `EolTester.slnx` (solution file định dạng mới, `dotnet` CLI đọc trực tiếp) ở thư mục gốc repo.

```powershell
# Build toàn bộ solution (Debug)
dotnet build EolTester.slnx -c Debug

# Chạy toàn bộ unit test (2 project: EolTester.Core.Tests, EolTester.Communication.Tests — xUnit)
dotnet test EolTester.slnx --no-build -c Debug

# Chạy 1 class/1 test cụ thể (khớp theo FullyQualifiedName)
dotnet test EolTester.slnx --filter "FullyQualifiedName~ModbusRegisterTableTests"
dotnet test EolTester.slnx --filter "FullyQualifiedName~ModbusWordAddressTests.Parse_BitAddress_ReturnsBitIndex"

# Chạy app ở chế độ Debug — LƯU Ý: mọi build đều bắt buộc kết nối PLC thật để hiển thị "Đã kết nối"
# (không còn Mock driver, xem mục 4) — chạy trên máy chưa đấu PLC sẽ luôn báo lỗi/Disconnected, không crash.
dotnet run --project src/EolTester.App/EolTester.App.csproj

# Publish Release self-contained ra thư mục APP/ (bản đóng gói đưa ra hiện trường)
dotnet publish src/EolTester.App/EolTester.App.csproj -c Release -r win-x64 --self-contained true -o APP
```

- Skill `publish-app` / agent `publisher` (`.claude/skills/publish-app/`, `.claude/agents/publisher.md`) tự động hóa đúng trình tự trên (dừng process cũ đang giữ file → build Debug → test → publish Release → smoke-test khởi động thử `APP/EolTester.App.exe`) — dùng khi cần cập nhật `APP/` sau khi code đã xác nhận đúng, không dùng để tự sửa lỗi code.
- Skill `verify-ui` tự động hóa việc chụp ảnh xác minh UI thật đúng chuẩn `PrintWindow` (không full-screen capture, xem Phần I mục 7) — dùng thay vì tự viết script chụp ảnh mỗi lần cần verify 1 thay đổi giao diện.
- `tmp/PlcReadProbe` (nằm trong `EolTester.slnx`) là project console nhỏ để tự thăm dò/đọc thanh ghi PLC thật ngoài luồng app chính — không phải một phần kiến trúc chính thức, chỉ dùng khi cần chẩn đoán địa chỉ D trực tiếp qua cổng COM.
- `test-rs485.ps1` ở thư mục gốc là script tiện ích build + chạy riêng `EolTester.Communication.Tests` (`.\test-rs485.ps1 [-Config Debug|Release] [-Verbose]`), có in tóm tắt tên các bộ test — **danh sách tên test trong script là text cố định viết tay, không phải kết quả tự dò**, nên vẫn còn nhắc tới `MockModbusDriverTests` dù driver này đã bị gỡ bỏ (xem mục 4); đừng dựa vào output đó để biết bộ test hiện có, chỉ dùng script này như một cách chạy nhanh `dotnet test` đã build sẵn.

## 5. Cấu trúc solution
```
/src
  EolTester.App                Ứng dụng WPF: Views, ViewModels, App.xaml, composition root (DI), Localization/ (Translation, TrExtension — xem mục 10), Resources/ (Strings.resx, Strings.en-US.resx), Interop/ (WindowAspectRatioLock — xem mục 12), Behaviors/ (KeyboardWedgeScanCapture — bắt phím cho đầu đọc barcode khi ô Scan có focus, MomentaryButton — xem mục 7 Tab 1/Tab 2), Services/AppPaths (thư mục cấu hình/log cạnh exe + fallback %LocalAppData% — xem mục 12b), SingleInstanceWarningWindow (chặn mở app 2 lần — xem mục 9), Services/CsvResultExportService (ghi CSV kết quả theo ngày — xem mục 7 Tab 4)
  EolTester.Core                 Model & interface thuần domain (TestStepDefinition, IoPointDefinition — có CommandAddress/HandoverAddress cho cấu trúc 3-bit Output, xem mục 7 Tab 2, JobInfo, TestResult...) — không phụ thuộc framework/IO
  EolTester.Communication        IPlcCommunicationDriver + các driver/: ModbusRtu/ (hiện tại), Mc/, Slmp/, CcLink/, EthernetIp/ (tương lai, cùng interface); ModbusWordAddress, ModbusRegisterTable (địa chỉ/bảng thanh ghi Dxxxx — xem mục 7); ModbusSlaveService, StreamAdapter, ModbusRegisterTableDataStore (PC làm Slave qua NModbus — xem mục 6)
  EolTester.Configuration        Đọc/ghi JSON: cấu hình kết nối, bản đồ I/O, profile spec/model, ngôn ngữ (language.json); CsvIoLabelService (Import/Export nhãn I/O runtime qua UI — xem mục 11); CsvSpecRegisterMapSource + SeedData/spec-register-map.csv (nguồn Address tĩnh cho 6 biến đo lường + các lệnh CMD_, không có UI Import/Export, overlay lại mỗi lần load — xem mục 7 Tab 4, docs/session-log/history.md mục 19); CsvIoMapSeedSource + SeedData/io-map-seed.csv (nguồn bản đồ I/O mặc định, KHÔNG có UI Import/Export, chỉ dùng đúng 1 lần lúc io-map.json chưa tồn tại — khác spec-register-map.csv, xem docs/session-log/history.md mục 19); JsonCsvExportSettingsStore (csv-export-settings.json — cấu hình xuất CSV kết quả, xem mục 7 Tab 4); JsonRuntimeStateStore (runtime-state.json — LastScanOk + số serial + ảnh chụp D100-D199, xem mục 12b); SeedData/users-seed.csv (seed tài khoản, xem mục 8); SeedFiles (4 file seed nhúng vào assembly làm bản gốc khôi phục — xem mục 12c)
  EolTester.Data                  Audit log + lịch sử kết quả/NG (SQLite)
  EolTester.Security              Xác thực, mô hình phân quyền (UserRole User/Operator/Admin), FileUserStore (nạp từ users.json, seed từ users-seed.csv — xem mục 8), băm mật khẩu (PBKDF2/BCrypt — không bao giờ lưu plaintext), Licensing/ (cấp phép bản quyền khóa theo máy — xem mục 9)
/tests
  EolTester.Core.Tests
  EolTester.Communication.Tests    gồm test encode/decode khung Modbus RTU, không cần phần cứng thật
/docs
  legacy-ui/                     4 ảnh tham khảo + ghi chú ngắn cho từng màn hình
```

## 6. Lớp giao tiếp — thiết kế độc lập giao thức
Đây là quy tắc kiến trúc quan trọng nhất của dự án này: **không ViewModel, View, hay logic nghiệp vụ nào được phép tham chiếu trực tiếp tới Modbus, `SerialPort`, hay bất kỳ kiểu dữ liệu đặc thù giao thức nào khác.** Mọi thứ ở tầng trên chỉ nói chuyện với `IPlcCommunicationDriver`.

```csharp
public interface IPlcCommunicationDriver : IAsyncDisposable
{
    ConnectionState State { get; }
    event EventHandler<ConnectionState> StateChanged;

    Task ConnectAsync(CancellationToken ct);
    Task DisconnectAsync();

    Task<bool> ReadDiscreteAsync(IoAddress address, CancellationToken ct);        // ngõ vào số (X)
    Task WriteDiscreteAsync(IoAddress address, bool value, CancellationToken ct); // ngõ ra số (Y)
    Task<int> ReadRegisterAsync(IoAddress address, CancellationToken ct);         // giá trị analog/thanh ghi
    Task WriteRegisterAsync(IoAddress address, int value, CancellationToken ct);

    // Đọc/ghi NHIỀU thanh ghi liên tiếp trong 1 giao dịch (Modbus FC03/FC16) — thêm sau khi phát hiện vòng
    // polling cũ (đọc/ghi từng thanh ghi lẻ) tốn ~240 giao dịch/tick, gây độ trễ hiển thị PLC→PC rất lớn.
    // Bộ gọi (PlcPollingService) tự chia nhỏ theo giới hạn Modbus (125 đọc/123 ghi) trước khi gọi.
    Task<int[]> ReadRegistersAsync(IoAddress startAddress, int count, CancellationToken ct);
    Task WriteRegistersAsync(IoAddress startAddress, IReadOnlyList<int> values, CancellationToken ct);
}
```
- `IoAddress` bọc một **chuỗi địa chỉ đặc thù theo giao thức** (ví dụ `"400001"` cho Modbus, `"X010"` cho kiểu địa chỉ thiết bị MC/SLMP) cùng với `IoPointDefinition.Key` logic mà nó thuộc về — nhờ vậy khi đổi driver chỉ cần map lại địa chỉ trong file cấu hình, không phải sửa UI/ViewModel.
- **`ModbusRtuDriver` thật đã viết xong** (NModbus trên nền `SerialPort`, dùng khi PC làm **Master**) — là driver DUY NHẤT, không còn `MockModbusDriver`/lựa chọn Mock-Real ở tab Setup (đã bỏ vì rủi ro vận hành, xem mục 4). Đã kiểm chứng kết nối thành công với PLC thật (xem mục 7 Tab 3, mục 13). Cả 3 driver (`ModbusRtuDriver`/`McProtocolDriver`/`SlmpDriver`) đều đã cài `ReadRegistersAsync`/`WriteRegistersAsync` — Modbus qua NModbus `ReadHoldingRegistersAsync`/`WriteMultipleRegistersAsync`, MC/SLMP qua `IMc3EFrameCodec.EncodeReadWordRequest(device, pointCount)` (vốn đã hỗ trợ multi-point sẵn, chỉ chưa được dùng tới trước đây).
- Các cài đặt dự kiến trong tương lai, cùng chung interface: `McProtocolDriver` (Mitsubishi MC dạng binary hoặc ASCII), `SlmpDriver`, `CcLinkDriver`, `EthernetIpDriver`. Được chọn lúc chạy qua factory dựa trên enum `ProtocolType` đọc từ cấu hình ở tab Setup — đây chính là lý do tab Setup tồn tại.
- Mọi lời gọi I/O đều `async`; một `PlcPollingService` chạy nền duy nhất sở hữu kết nối và chuyển dữ liệu cập nhật sang UI thread — không bao giờ block UI thread khi giao tiếp serial, không bao giờ gọi `.Result`/`.Wait()`.
- Có timeout + retry giới hạn có backoff cho mọi request; `ConnectionState` (Disconnected/Connecting/Connected/Error) được expose như một property có thể quan sát, bind vào chỉ báo trạng thái cố định trong khung ứng dụng — đúng là khu vực "setup model và Status" mà người dùng mô tả là dùng chung xuyên suốt mọi tab.
- **Mất kết nối PLC giữa chừng không được phép crash app** — bug thật đã xảy ra: `[RelayCommand]` sinh `ICommand.Execute` dạng `async void`, nên 1 exception ném ra từ driver (VD `IOException` khi cáp bị rút giữa lúc operator bấm Start/Reset/Xác nhận NG/ON-OFF thủ công) bay thẳng qua `SynchronizationContext` lên Dispatcher và crash cả app nếu không ai bắt. Đã vá 2 lớp:
  1. `PlcPollingService.WriteBitAsync`/`WriteRawWordAsync` (gateway duy nhất mọi lệnh điều khiển đi qua) tự bọc try/catch, log lỗi và nuốt exception thay vì ném ra ngoài — mọi `PulseCommandAsync`/`SetLevelCommandAsync`/`WriteOutputAsync`/Giám sát DATA write đều thừa hưởng, không cần sửa từng nơi gọi riêng lẻ.
  2. `App.OnStartup` đăng ký `DispatcherUnhandledException` — lưới an toàn cuối cho những trường hợp chưa lường hết, chỉ log + `e.Handled = true`, không crash.
  - **Ngoài ra, các nút gửi lệnh (Start/Stop, Reset, Xác nhận NG ở footer; ON/OFF thủ công + AUTO/MANUAL ở tab Monitor) tự động disable khi `ConnectionState == Error`** — `ShellViewModel.CanSendCommands`/`MonitorTabViewModel.CanSendCommands`, cùng công thức, bind `IsEnabled`. CỐ Ý không disable ở `Disconnected`/`Connecting` — `Disconnected` là trạng thái bình thường của Role=Slave (không polling chủ động) và của driver trước khi `StartAsync` chạy xong, disable nhầm sẽ khóa cứng nút ở chế độ hoàn toàn hợp lệ.
  - **ĐÃ SỬA bug thật: huy hiệu kết nối bị kẹt ở "Chưa kết nối" khi Role=Slave dù `ModbusSlaveService` đã chạy đúng** — nguyên nhân: `ShellViewModel.ConnectionState`/`MonitorTabViewModel` trước đây chỉ subscribe `PlcPollingService.StateChanged`, mà `PlcPollingService.State` không bao giờ đổi khi Slave (Slave không tự polling, không sở hữu driver Master). Đã thêm `ShellViewModel.ObserveSlaveConnectionState(IPlcSlaveService)`/`MonitorTabViewModel.ObserveSlaveConnectionState(...)` — bind trực tiếp vào `ModbusSlaveService.State`/`StateChanged` khi Role=Slave, gọi từ `App.xaml.cs` ngay sau khi resolve `_activeSlaveService`, trước khi `StartAsync()`.

### PC làm Modbus Slave — kiến trúc song song, chuyển đổi được với Master
Quản lý xác nhận PLC thực tế sẽ làm **Master quản lý nhiều thiết bị RS-485 khác** — PC (705/715 EOL Tester) phải là 1 **Slave** trong số đó, đảo ngược quyết định trước ("PC luôn Master, loại hẳn Slave"). Đã khôi phục `ConnectionSettings.Role` (`ModbusRole.Master`/`Slave`, `EolTester.Configuration.Models`) + `MySlaveId` (Slave ID của chính PC, khác `StationAddress` = Slave ID của PLC khi Master).

- **Chỉ 1 trong 2 vai trò chạy tại 1 thời điểm** — RS-485/Modbus RTU chỉ cho phép 1 master trên bus. `App.xaml.cs` đọc `ConnectionSettings.Role` lúc khởi động: Master → `PlcPollingService.StartAsync()` (chủ động polling như cũ); Slave → `PlcPollingService.LoadIoMapOnlyAsync()` (chỉ nạp danh sách I/O để tab Monitor vẫn hiển thị được, không polling) + `ModbusSlaveService.StartAsync(...)`. **Đổi Role ở Setup yêu cầu khởi động lại app** để áp dụng — chưa hỗ trợ hot-swap runtime (quyết định phạm vi, xem docs/session-log/history.md mục 15).
- **`ModbusSlaveService`** (`EolTester.Communication/ModbusSlaveService.cs`) — dùng thư viện **NModbus** (`PackageReference NModbus`, thêm ở round này), tạo `ModbusFactory().CreateRtuSlaveNetwork(stream)` rồi `network.ListenAsync(ct)` chạy trên `Task.Run` nền riêng, hoàn toàn bị động (không phải vòng lặp polling như Master).
- **`StreamAdapter`** (`IStreamResource` của NModbus) bọc bất kỳ `System.IO.Stream` nào — cổng COM thật qua `SerialPort.BaseStream`, hoặc `NetworkStream` dùng cho test tự động qua TCP loopback. NModbus **không phụ thuộc trực tiếp `SerialPort`**, đây là phát hiện quan trọng: cho phép viết integration test hoàn toàn tự động (2 test trong `ModbusSlaveServiceTests.cs`, dùng `TcpListener`/`TcpClient` loopback làm đường truyền song công giả lập) **mà không cần cài driver COM ảo (com0com)**.
- **`ModbusRegisterTableDataStore`** (`ISlaveDataStore` của NModbus) — cầu nối 2 chiều với `ModbusRegisterTable` (đã có từ round trước): `HoldingRegisters`/`InputRegisters` implement `IPointSource<ushort>`, đọc/ghi thẳng qua `ModbusRegisterTable.TryGetWord`/`TryUpdateWord` **theo thời gian thực** (NModbus gọi `ReadPoints`/`WritePoints` đúng lúc PLC hỏi/ghi tới) — **không cần đồng bộ định kỳ qua timer** như dự tính ban đầu trong kế hoạch, đơn giản hơn nhiều. Coil rời (`CoilDiscretes`/`CoilInputs`) dùng mảng `bool[]` riêng trong bộ nhớ, chưa liên kết với `ModbusRegisterTable` — chưa có nhu cầu cụ thể, mở rộng khi cần.
- **`ModbusRegisterTable` là thread-safe** (bọc `System.Threading.Lock`) — bắt buộc vì giờ có 2 nguồn truy cập đồng thời (luồng lắng nghe Slave + luồng UI/nghiệp vụ).
- **Setup tab**: `ComboBox` Role (Master/Slave) — khi Slave, ẩn `StationAddress`/`PollingInterval`/`Timeout-Retry`/khối Input-Output/nút Test Connection (không còn ý nghĩa vì Slave không chủ động polling), hiện `MySlaveId` + ghi chú giải thích chế độ; ghi chú màu cam "Đổi Vai trò cần khởi động lại".
- **Bẫy đã gặp lúc viết integration test**: `NetworkStream`/`SerialPort.BaseStream` mặc định `ReadTimeout`/`WriteTimeout` = infinite (`-1`) — nếu không set tường minh, `NModbus` request-response có thể **treo vô hạn** khi có bất kỳ trục trặc giao tiếp nào (không ném exception, không timeout, im lặng chờ mãi). Luôn set `ReadTimeout`/`WriteTimeout` tường minh (VD `3000`ms) trên `IStreamResource`, kể cả khi test.

## 7. Đặc tả từng màn hình

### Khung ứng dụng dùng chung (hiển thị xuyên suốt cả 4 tab)
- **Phần đầu**: tiêu đề ứng dụng/sản phẩm, thanh tab (Trang Tự động / Giám sát-Thủ công / Cài đặt-Giới thiệu / Set spec.), ô mật khẩu (dùng `PasswordBox` để che ký tự) + Đăng nhập/Đăng xuất bind vào service xác thực/phân quyền.
- **Phần cuối**: thông tin Lot (Model / Ngày / Job → ghép thành Job ID), "XÁC NHẬN JOB", đồng hồ cycle time, "Phát hiện NG" / "XÁC NHẬN SẢN PHẨM LỖI", Scan Barcode (mã hiện tại/mã ghi nhận), trạng thái Start/Waiting, nút Reset.
- **Bổ sung mới**: chỉ báo trạng thái kết nối PLC cố định (mục 6) thuộc về khung dùng chung này, không đặt riêng lẻ trong từng tab.

### Tab 1 — Trang Tự động (Main/Auto)
- Bên trái: bảng kết quả bước kiểm tra, **bind dữ liệu** từ `IReadOnlyList<TestStepResult>` (Bước, Mô tả, Giới hạn dưới, Giá trị, Giới hạn trên), chỉ đọc; giới hạn lấy từ profile spec đang active (chỉ sửa được ở tab Set spec.), giá trị lấy realtime từ polling service. Cấu trúc bước có khung cố định gồm **High mode + Low mode**, mỗi mode gồm các phép đo cùng dạng (điện áp/dòng điện/lực hút/LED); chỉ giá trị giới hạn thay đổi theo model — vẫn phải data-driven (không hardcode dòng UI), nhưng model dữ liệu có thể giả định khung 2-mode này thay vì hoàn toàn tự do.
- Ở giữa: chỉ báo tín hiệu **LED 1 / LED 2 / LED 3** (`SIGNAL_LED1`=`D70.0`, `SIGNAL_LED2`=`D70.1`, `SIGNAL_LED3`=`D70.5` — bit `.5` trước đây trống, thêm theo yêu cầu người dùng), chỉ báo cảm biến phát hiện sản phẩm (`SIGNAL_PRODUCT_DETECT`=`D70.2`) — tất cả bind qua `PlcPollingService.Led1/Led2/Led3/ProductDetected` (`SignalIndicatorState`, đọc bit `Dxxxx.Y` từ `spec-register-map.csv` bằng `UpdateSignal` level-based mỗi tick), không phải control hardcode. Thêm 1 LED nữa sau này = thêm 1 dòng `SIGNAL_LEDn` vào CSV + 1 property `Ledn` ở `PlcPollingService`/`MainTabViewModel` + 1 khối Grid ở `MainTabView.xaml` + key resx `Main_Ledn`.
- Bên phải: **1 logo ảnh thật** — `Assets/Logo1.png` (TTI) trong khung chữ nhật cố định 210×110, `Stretch="Uniform"`. `Logo2.png` (ALPHAVINA) đã gỡ khỏi cả header (`MainWindow.xaml`) lẫn khung phải (`MainTabView.xaml`) theo yêu cầu người dùng — file vẫn còn trong `Assets/`, chỉ không tham chiếu. Icon cửa sổ/taskbar khi app chạy = `Assets/Logo3.png` (giờ là logo màn hình/nhịp tim 512×512, copy từ `logo31.png` — trước đây là ảnh trắng); icon file .exe = `Assets/Logo3.ico` (cùng logo, `<ApplicationIcon>` trong `.csproj`). **Còn nợ:** đường dẫn ảnh vẫn hardcode trong XAML, cần chuyển sang cấu hình khi có nhiều khách hàng khác nhau. Kế tiếp: khối thông tin yêu cầu scan (mã OK gần nhất, serial gần nhất/tiếp theo, checkbox bắt buộc đúng thứ tự tem), lịch sử thông báo/lỗi bind từ cùng audit/event log ở mục 3 (lọc theo các mục liên quan tới người vận hành, gần đây nhất).
- **Scan Barcode — bắt phím khi ô "Mã Scan quét được" đang focus, 3 chế độ nhận diện cấu hình được ở Set Spec.** (`TestParameters.ScanMode` — enum `BarcodeScanMode { PrefixStripped, PrefixKept, FixedLength }`, `Setting_ScanMode` ComboBox + `Setting_ScanPrefixText` TextBox ở tab Set Spec.):
  - `KeyboardWedgeScanCapture.Attach(window, viewModel, scanBox)` (`EolTester.App/Behaviors/KeyboardWedgeScanCapture.cs`) hook `PreviewTextInput`/`PreviewKeyDown` ở cấp `Window` (`handledEventsToo: true`) nhưng **bỏ qua hoàn toàn nếu `scanBox` (ô `x:Name="ScanInputBox"`) không có keyboard focus** (`scanBox.IsKeyboardFocused`) — sửa bug thật "không gõ được gì trên bàn thử" (trước đây bắt toàn cục, ở SCAN MODE + máy chưa sẵn sàng thì nuốt mọi ký tự vào login/Job/Setup). Khi ô Scan có focus: đọc **live** `SettingTab.ScanMode`/`ScanPrefixText`/`ScanCodeLength` mỗi lần quyết định (đổi cấu hình giữa phiên có hiệu lực ngay). Đánh đổi: quét khi focus ở ô khác → ký tự vào ô đó (FixedLength thừa 1 ký tự → quét lại).
  - **PA1 (PrefixStripped)/PA2 (PrefixKept)**: dò khớp tiền tố ký tự-theo-ký tự (state machine `Idle → PendingPrefixMatch → Capturing`), `e.Handled = true` suốt quá trình dò khớp/capturing để không ký tự nào lọt vào control đang focus; Enter → commit, Escape → hủy. Tiền tố 1 ký tự (khuyến nghị, VD backtick `` ` ``) đảm bảo an toàn tuyệt đối; tiền tố dài hơn có đánh đổi nhỏ đã thống nhất với người dùng (xem docs/session-log/history.md).
  - **PA3 (FixedLength)**: nhận diện theo tốc độ gõ (khoảng cách giữa các ký tự liên tiếp rất ngắn, VD <50ms) thay vì ký tự đánh dấu — tự commit ngay khi đủ `ScanCodeLength` ký tự, không cần Enter. Đánh đổi đã chấp nhận: ký tự đầu của 1 lần quét có xác suất rất nhỏ lọt vào ô đang focus trước khi hệ thống kịp nhận diện chuỗi gõ nhanh bất thường.
  - `ShellViewModel.CommitScanAsync()` (`[RelayCommand]`, đổi từ `CommitScan()` đồng bộ sang async để chạy được pipeline SCAN MODE bên dưới — `IAsyncRelayCommand` sinh ra vẫn tương thích ngược 100% với mọi `Command="{Binding CommitScanCommand}"`/`Execute(null)` hiện có) là điểm chốt duy nhất cho mọi đường vào (cả 3 chế độ scan lẫn gõ tay + Enter qua `KeyBinding` trên ô "Mã Scan quét được", lẫn `SIGNAL_SCAN_CONFIRM` từ PLC) — trim, gán `LastScanOk`, xóa buffer, ghi notification (REV MODE) hoặc chạy thêm pipeline kiểm tra (SCAN MODE, xem bên dưới). Thay thế `OnScanBarcodeTextChanged` cũ (đã xóa) vốn commit trên mỗi ký tự gõ do `UpdateSourceTrigger=PropertyChanged` — bug thật: chuỗi nhiều ký tự chỉ ký tự cuối được giữ lại. Toàn thân bọc try/catch (log + Notification), không để exception bay qua `ICommand.Execute` làm crash app (đúng nguyên tắc CLAUDE.md mục 6).
  - **PLC-triggered move sang "Mã Scan ghi nhận"**: `SIGNAL_SCAN_CONFIRM` (`spec-register-map.csv`, `D70.3`) — PLC tự chuyển 0→1 rồi tự tắt, PC chỉ đọc (rising-edge detection, `PlcPollingService.UpdateEdgeSignal`, khác `UpdateSignal`/`UpdateWordValue` level-based hiện có) → raise `ScanConfirmSignalRaised` → `ShellViewModel` subscribe (fire-and-forget), gọi `CommitScanAsync()`. Cho phép PLC chủ động quyết định thời điểm "chốt" giá trị đang quét (thay vì chỉ dựa vào Enter/độ dài phía PC).
  - **"Cho phép sửa Barcode"** (`TestParameters.AllowEditBarcode` — `PARAM_EDIT_SCAN`, `D104.0`, checkbox ở tab Set Spec., đổi tên+ý nghĩa từ `RequireScan`/`PARAM_REQUIRE_SCAN` cũ vốn không có tác dụng gì phía PC, chỉ đẩy xuống PLC): khóa/mở khóa việc gõ tay vào ô "Mã Scan quét được" — `false` (mặc định): `TextBox.IsReadOnly=True` (bind `SettingTab.AllowEditBarcode` qua `InverseBooleanConverter`, `MainWindow.xaml`), ô chỉ nhận giá trị từ đầu đọc qua `KeyboardWedgeScanCapture` (ghi thẳng vào `ScanBarcodeText` qua binding, không đi qua đường gõ tay nên không bị `IsReadOnly` chặn), gõ tay bị vô hiệu hoàn toàn, muốn đổi giá trị phải quét lại; `true`: gõ tay bình thường, commit qua `KeyBinding` Enter hiện có hoặc qua hoàn thành 1 lần quét — đúng hành vi mặc định trước khi có tính năng này. Vẫn đẩy `PARAM_EDIT_SCAN` xuống PLC như `RequireScan` cũ (PLC có thể tự dùng theo ý riêng), nhưng giờ **có tác dụng thật ở phía PC** (trước đây hoàn toàn không). Từ khi có SCAN MODE/REV MODE (bên dưới), riêng checkbox NÀY **bị khóa và tự ép `false` khi đang SCAN MODE** — chỉ tự do bật/tắt khi ở REV MODE. ("Bắt buộc đúng thứ tự tem" và "Check Duplicate in log" **KHÔNG** còn bị ép/khóa — xem SCAN MODE bên dưới.)
  - **SCAN MODE / REV MODE** (`TestParameters.ScanRevMode` — `PARAM_SCAN_REV_MODE`, `D104.4`; `false`=SCAN MODE mặc định, `true`=REV MODE; 2 nút set trực tiếp ở tab Set Spec., cùng hàng "MODE TEST", pattern y hệt `SetTestOrder`/`TestOrderLowToHigh`): biến luồng quét barcode ở footer thành **tự động hoàn toàn**, thay operator tự bấm Start sau khi quét — tính năng lớn, ảnh hưởng trực tiếp vận hành dây chuyền thật, cần xác nhận trên bàn thử nghiệm PLC thật trước khi coi là hoàn thiện (chưa test end-to-end với PLC thật, chỉ verify được logic/UI qua code + unit test + UI tĩnh).
    - **Vào SCAN MODE chỉ tự ép** `AllowEditBarcode=false` (`SettingTabViewModel.OnScanRevModeChanged`/`ApplyScanModeInvariant` — **gọi tường minh cả lúc load**, vì `[ObservableProperty]` bỏ qua raise khi gán trùng default). Riêng checkbox "Cho phép sửa Barcode" bị khóa (`IsEnabled="{Binding ScanRevMode}"`). **"Bắt buộc đúng thứ tự tem" (`PARAM_REQUIRE_SCAN_ORDER`, D104.1) và "Check Duplicate in log" (`PARAM_CHECK_DUPLICATE_LOG`, D104.5, mặc định `true`) là 2 option ĐỘC LẬP** — SCAN MODE không ép, không khóa; chỉ được đọc khi pipeline SCAN MODE chạy. REV MODE **không ép/tự động gì**.
    - **Cổng vào (`KeyboardWedgeScanCapture.OnTextInput`)**: SCAN MODE chặn mọi ký tự quét (kể cả ký tự đầu FixedLength) trừ khi `PlcPollingService.IsMachineReadyForScan()` — cache `SIGNAL_MACHINE_WAITING` (`D72.0`) VÀ `CMD_START` (`D100`) đều bằng 0. Chưa có dữ liệu → mặc định KHÔNG sẵn sàng. REV MODE không bị chặn. (Cổng này chỉ chạy khi ô Scan có focus — xem đầu mục.)
    - **`ShellViewModel.CommitScanAsync()` (SCAN MODE) — pipeline theo thứ tự** (fail bất kỳ bước nào: chỉ `AddNotification` nêu lý do, **không xóa `ScanBarcodeText`**, không commit, không gửi CMD_START):
      1. **Check 4 (LUÔN, đầu tiên)**: `trimmed` phải khác `LastScanOk` (mã quét được ≠ mã ghi nhận gần nhất). Trùng → ghi `PARAM_SCAN_DUP_ERROR` (`D104.6`) = **1** + `Notify_ScanRejectedSameAsLast`, dừng.
      2. **Check 1 (LUÔN)**: `BarcodeSerialExtractor.TryExtractSerialNumber` (`EolTester.Core`, unit-test) trích số thứ tự theo `SerialStartIndex`/`SerialEndIndex` (1-based).
      3. **Check 2 (chỉ khi `RequireScanOrder`)**: serial = `_previousSerialNumber + 1` (`_previousSerialNumber` reset `null` mỗi khi chuyển VÀO SCAN MODE + persist qua `runtime-state.json`, xem mục 12b; null → luôn pass).
      4. **Check 3 (chỉ khi `CheckDuplicateInLog`)**: `IBarcodeScanLogService.IsRecordedTodayAsync` — chưa có trong `scanlog\barcode-<ngày>.log`.
      - Toàn bộ pass → commit (`LastScanOk`, xóa buffer) + ghi `PARAM_SCAN_DUP_ERROR` = **0** (chỉ về 0 khi TOÀN BỘ pipeline pass) + `PulseCommandAsync("CMD_START", 1000)` + `IsRunning=true`.
    - **Bản đồ bit từ D104** (`spec-register-map.csv`, tất cả PC→PLC, chưa test PLC thật): `.0` `PARAM_EDIT_SCAN` (Cho phép sửa Barcode) · `.1` `PARAM_REQUIRE_SCAN_ORDER` (Bắt buộc đúng thứ tự tem) · `.2` `PARAM_SCAN_CODE_READY` (SCAN MODE: theo xung `CMD_START`; REV MODE: theo mức "LastScanOk khác rỗng") · `.3` `PARAM_TEST_ORDER` (thứ tự High↔Low) · `.4` `PARAM_SCAN_REV_MODE` (SCAN/REV) · `.5` `PARAM_CHECK_DUPLICATE_LOG` (Check Duplicate in log) · `.6` `PARAM_SCAN_DUP_ERROR` (cờ lỗi trùng mã ghi nhận). Trống: `.7`–`.15`.
    - **Chống trùng barcode tách khỏi CSV export (`IBarcodeScanLogService`/`BarcodeScanLogService`, `EolTester.App/Services/`)** — trước đây dedup dựa vào việc dò cột `<barcode>` trong file CSV kết quả "đang hoạt động"; sau khi cột CSV trở thành tự do (Admin tự sắp xếp/bỏ token), coupling này có 2 lỗ hổng: (1) chưa cấu hình/tắt thư mục xuất CSV → dedup lặng lẽ luôn pass, mất hẳn bảo vệ chống trùng mà không cảnh báo gì (rủi ro chất lượng thật, vì CSV export là tính năng **option** theo mục 4); (2) đổi cấu hình cột giữa ngày làm CSV tách sang file `-n` khác, dedup chỉ còn thấy đúng file đang hoạt động. Đã tách hẳn thành file text riêng `scanlog/barcode-<yyyyMMdd>.log` (`AppPaths.ScanLogDirectory`, cạnh file .exe — cùng pattern `IoLabelExportDirectory`), mỗi dòng 1 barcode, không phụ thuộc cấu hình cột CSV nào. Giữ tối đa 100 file (~3 tháng, mỗi ngày 1 file) — quá số lượng thì tự xóa file cũ nhất theo ngày trong tên file, dọn dẹp 1 lần mỗi khi sang ngày mới (không quét thư mục mỗi lần ghi/kiểm tra). **Thời điểm ghi nhận GIỮ NGUYÊN như thiết kế cũ** (đã xác nhận với người dùng, không đổi): lúc PLC báo `SIGNAL_CSV_WRITE` (1 chu trình test đã hoàn tất, cùng lúc với `AppendCsvRowSafeAsync`/`ShellViewModel.RecordScanLogSafeAsync`, 2 tác vụ độc lập không ảnh hưởng lẫn nhau khi 1 bên lỗi) — KHÔNG phải ngay lúc quét; barcode của 1 chu trình chưa hoàn tất (máy dừng giữa chừng, lỗi, PLC không bắn tín hiệu) không bị coi là đã dùng, vẫn quét lại được cùng ngày. `CsvResultExportService` đã bỏ hẳn logic dedup/`ParseCsvLine` (không còn cần — cache chỉ còn đếm dòng cho `<STT>`).
- **Khối footer là MỘT khối chữ nhật duy nhất** gồm 3 cột kế nhau, cùng chiều cao (khung `Border` ngoài cùng cao cố định, ví dụ 240px), phân cách bằng đường kẻ dọc mảnh:
  - **Cột 1 — "THÔNG TIN LOT"**: bảng 2 cột theo đúng bố cục bản gốc — cột 1 là nhãn (Model/Ngày/Job/Job ID), cột 2 là ô giá trị, mỗi trường một hàng xếp dọc, nút "XÁC NHẬN JOB" dưới cùng. (Đã chốt qua phản hồi thực tế.)
  - **Cột 2 — "TRẠNG THÁI"** (cao bằng cột 1): 3 hàng dọc — (1) Cycle time định dạng **hh:mm:ss** (dữ liệu PLC trả về là giây, ViewModel tự chuyển đổi sang `TimeSpan` rồi format hiển thị); (2) ô "PHÁT HIỆN HÀNG NG" đổi màu **đỏ** khi bit NG/OK từ PLC báo NG, màu xám trung tính khi bình thường; (3) nút "XÁC NHẬN HÀNG NG" — dạng **giữ = ON, thả = OFF** (`MomentaryButton` attached behavior, xem mục 7 cuối) thay vì click cố định: nhấn xuống ghi `true` vào `CMD_CONFIRM_NG` (đồng thời xóa cờ NG + ghi thông báo ngay lúc nhấn, không chờ thả), thả ra ghi `false`. Style `IsPressed` đổi màu khi giữ vẫn còn nhưng **hiện màu xanh dương nhạt thay vì LimeGreen như mô tả trước** — hành vi chrome mặc định của Button đè lên trigger, đã phát hiện qua verify UI thật, chưa sửa (chỉ là vấn đề màu sắc, không ảnh hưởng chức năng — xem mục 13).
  - **Cột 3 — chiếm hết chiều rộng còn lại**, chia dọc thành 2 khối theo `RowDefinition Height="*"` (Scan Barcode, chiếm phần lớn) + `Height="Auto"` (Operation, dải ngang mỏng phía dưới) — bố cục này đã chỉnh lại theo ảnh mockup người dùng tự edit (`Test/Report/screenshots/monitor-edit.png`), thay cho tỉ lệ 46%/54% cố định ban đầu:
    - **"SCAN BARCODE"**: 2 trường song song, mỗi trường là một `TextBox` có viền rõ ràng (không phải chữ trơn) — "Mã Scan quét được" (ô nhập nhận tín hiệu từ máy quét USB keyboard-wedge) và "Mã Scan ghi nhận" (`IsReadOnly`, giá trị đã xác nhận gần nhất). **Không còn checkbox "Bắt buộc đúng thứ tự tem" ở đây** — đã xóa bản trùng lặp thuần UI (không lưu/đẩy PLC) này, chỉ còn đúng 1 nguồn ở tab Set Spec. (`SettingTabViewModel.RequireScanOrder`).
    - **"OPERATION"**: một dải ngang mỏng ở cuối cột — nhãn "OPERATION" nhỏ bên trái, kế đó là trạng thái vận hành dạng chữ thường cỡ lớn (`OperationStatusText` — "WAITING START" khi dừng, "RUN AUTO" hoặc "RUN MANUAL" khi đang chạy tùy `MonitorTab.Mode`, không còn bọc trong khung màu), rồi nút Start/Stop (đổi nhãn + màu theo `IsRunning`) và nút Reset đẩy sát mép phải.
  - **Start/Stop giữ nguyên dạng click** (theo yêu cầu rõ ràng của người dùng — đây là lệnh điều khiển quy trình, không phải điều khiển vật lý trực tiếp 1 bit, máy cần tiếp tục chạy sau khi thả chuột): Start khi đang dừng → gửi xung `CMD_START` qua `PulseCommandAsync`, đổi nhãn "STOP"; bấm lần nữa → gửi xung `CMD_STOP`. **Reset đã đổi sang dạng giữ = ON, thả = OFF** (`MomentaryButton`, xem mục 7 cuối) — ghi `true` vào `CMD_RESET` lúc nhấn xuống (đồng thời đưa NG/cycle time/IsRunning về 0 ngay lúc nhấn), ghi `false` lúc thả ra, qua `PlcPollingService.SetLevelCommandAsync` (khác `PulseCommandAsync` — không tự đảo về false sau 1 khoảng cố định, mà chờ đúng lúc nhả nút). Cả `SetLevelCommandAsync` lẫn `WriteOutputAsync` (mục dưới) đều tự đảm bảo **xung tối thiểu 200ms** xuống PLC dù người dùng bấm/thả nhanh hơn — nếu nhả trước khi đủ 200ms tính từ lúc nhấn, code tự `Task.Delay` phần còn thiếu trước khi ghi `false`, tránh PLC quét không kịp thấy xung quá ngắn. `CMD_START`/`CMD_STOP`/`CMD_RESET`/`CMD_CONFIRM_NG` đã map thật qua `spec-register-map.csv` (không còn là khóa logic tạm trong `MockModbusDriver`) — địa chỉ D thật vẫn cần đối chiếu khi có bảng địa chỉ PLC chính thức.
  - **Hàng hành động dưới cùng thẳng hàng, lấy nút "XÁC NHẬN JOB" làm chuẩn**: footer Grid có 2 hàng — hàng nội dung (`*`) và hàng hành động (`Auto`, dùng chung cho cả 3 cột). Nút "XÁC NHẬN JOB" (cột 1), nút "XÁC NHẬN HÀNG NG" (cột 2), và dải "OPERATION" + Start/Reset (cột 3) đều nằm ở hàng `Auto` này với `MinHeight="38"` — nhờ cùng một `Grid.Row` nên tự động cùng chiều cao, cùng đường căn dưới, tạo thành 1 hàng ngang xuyên suốt chiều rộng footer. Ô "PHÁT HIỆN HÀNG NG" ở cột 2 giờ nằm trong hàng nội dung riêng với `RowDefinition Height="*"`, giãn cao lấp khoảng trống do nút xác nhận đã chuyển xuống hàng hành động chung.
  - Sau khi gộp hàng hành động, tổng chiều cao footer giảm (Border ngoài `MinHeight="150"` thay vì cố định 240px trước đó) — khoảng trống dư ra được dùng để **tăng `RowHeight`/`ColumnHeaderHeight` của bảng Input/Output ở tab Monitor** (25/26 thay vì 21/24 trước đó) cho dễ đọc hơn, thay vì tăng vùng giữa của tab Main (bảng High/Low mode có số dòng cố định, không tận dụng thêm được không gian). Cửa sổ `Window` vẫn giữ nguyên `Height="800"` — không cần co lại vì phần thừa đã dùng hết cho Monitor.

### Tab 2 — Giám sát/Thủ công (Monitor & Manual)
- **Chuyển chế độ Tự động/Thủ công bằng 2 nút độc lập** ("TỰ ĐỘNG" / "THỦ CÔNG"), không phải 1 nút gộp — nút tương ứng trạng thái hiện tại được tô xanh (`LimeGreen`), nút kia giữ màu trung tính. **Yêu cầu đăng nhập tối thiểu quyền User** để chuyển chế độ hay dùng điều khiển thủ công (xem mục 8 — khách chưa đăng nhập chỉ chuyển tab + quét barcode); `MonitorTabViewModel.CanSendCommands` = `_canOperate && ConnectionState != Error`. **Mỗi lần đổi chế độ giờ ghi 1 thanh ghi mức xuống PLC** — `CMD_MODE` (`spec-register-map.csv`, gộp từ 2 dòng `CMD_MODE_AUTO`/`CMD_MODE_MANUAL` cũ thành 1 dòng duy nhất theo yêu cầu người dùng): `false`/0 = Auto, `true`/1 = Manual (**giả định**, đảo cực dễ dàng khi có PLC thật). Gọi qua `PlcPollingService.SetLevelCommandAsync("CMD_MODE", ...)` ngay sau khi đổi `Mode`, chạy ở cả 2 chiều chuyển đổi — độc lập với bumpless-transfer (`TransferHandoverToCommandAsync`, chỉ chạy khi chuyển sang Manual, giữ nguyên logic riêng).
- Bảng tín hiệu ngõ vào và bảng tín hiệu ngõ ra đều **bind dữ liệu từ bản đồ I/O** (`IReadOnlyList<IoPointDefinition>` lọc theo Direction), không phải các dòng cố định trong XAML — đây chính là điều giúp việc đổi PLC/giao thức sau này (mục 6) chỉ cần sửa cấu hình.
- **Thứ tự hiển thị điểm I/O theo địa chỉ bát phân (octal) kiểu Mitsubishi**: X00–X07 rồi **X10–X17** (không có X08/X09 — bit thứ 8 nhảy sang "hàng chục" theo octal), tương tự cho Y00–Y07/Y10–Y17. Seed mặc định ở `JsonIoMapStore.CreateSeedProfile()` sinh Key theo `Convert.ToString(bitIndex, 8).PadLeft(2,'0')` để đảm bảo đúng thứ tự và đúng định dạng chuỗi này; đây là quy ước hiển thị/đặt tên, không phải bản đồ địa chỉ Modbus thật (vẫn còn ở mục 12).
- **Cột "Điểm" đổi màu xanh (nền `LimeGreen`, chữ trắng) khi tín hiệu đang ON** — giúp operator nhận biết trạng thái ngay trong nháy mắt, dùng `DataGridTextColumn.ElementStyle` với `DataTrigger` theo `Value`, áp dụng cho cả hai bảng Input/Output. (Cột "Giá trị" riêng đã bỏ hẳn — xem mục 11.)
- **Điều khiển ngõ ra thủ công dùng 2 nút riêng "ON"/"OFF"** — nút khớp với Bit 2 (`CommandValue`, lệnh PC vừa gửi — KHÔNG phải Bit 1/`Value`) được tô xanh, giúp operator luôn thấy rõ lệnh mình vừa gửi là gì (xem chi tiết tách bạch Bit 1/Bit 2 ở cấu trúc 3-bit bên dưới). Cả hai nút bị vô hiệu hóa khi đang ở chế độ Tự động HOẶC khi chưa đăng nhập (`CanUseManualControls = Mode==Manual && CanSendCommands`, `CanSendCommands` giờ gồm cả `_canOperate`), và mọi lệnh thủ công vẫn phải ghi một dòng audit log (ai — hoặc "?" nếu chưa đăng nhập, điểm nào, giá trị gì, lúc nào).
  - **Cả 2 nút ON/OFF đều dạng click-toggle-latch thường** (`Command`/`CommandParameter` bình thường, KHÔNG dùng `MomentaryButton`) — click ON thì giữ nguyên ON tới khi click OFF, và ngược lại. Đã thử đổi ON sang dạng "giữ = ON, thả = OFF" (`MomentaryButton` — attached behavior còn giữ trong `EolTester.App/Behaviors/MomentaryButton.cs`, hiện chỉ còn dùng cho Reset/Xác nhận NG ở footer, xem dưới) nhưng người dùng yêu cầu **revert lại nguyên trạng click-toggle** cho riêng khối "Điều khiển thủ công" này ngay sau đó. **Guard hiện tại (`TurnOnAsync`/`TurnOffAsync`) chỉ còn `if (point is null || !CanUseManualControls) return;`** — không còn dựa vào `point.Value`/Bit 1 để né ghi lặp như thiết kế ban đầu: sau khi Bit 1 tách hẳn độc lập khỏi Bit 2 (không còn tự đổi theo lệnh vừa gửi), dùng nó làm điều kiện chặn nút OFF từng gây lỗi "bấm OFF không có tác dụng" — ghi lặp cùng giá trị xuống Bit 2 vô hại nên bỏ hẳn phần né ghi lặp thay vì thêm 1 state cục bộ khác.
  - **`WriteOutputAsync` vẫn còn logic đảm bảo xung tối thiểu 200ms** (`RecordPress`/`DelayForMinPulseAsync`, dùng chung với `SetLevelCommandAsync`) — vô hại với click-toggle độc lập (chỉ có tác dụng nếu 2 lần ghi cách nhau dưới 200ms, không xảy ra khi bấm ON rồi OFF ở 2 thời điểm tách biệt), không cần gỡ bỏ.
  - **Bit 1 polling cho Output đã được bật lại** — round trước đã tắt hẳn polling Bit 1 (`Address`) cho Output có `CommandAddress` vì nhiễu ngẫu nhiên blanket của `MockModbusDriver` ghi đè giá trị lạc quan sau khi bấm ON/OFF. Sau khi nhiễu chuyển sang opt-in theo địa chỉ (`ConfigureJitterAddresses`, chỉ áp cho 6 thanh ghi đo lường), Bit 1 của Output (D1003/D1004...) không còn bị nhiễu, nên `PollLoopAsync` poll Bit 1 cho **mọi** Output giống hệt Input — "Điểm" luôn phản ánh đúng Bit 1, và **đồng bộ được với "Giám sát DATA" ở tab Setup** (sửa D1003 ở đó → Monitor tự cập nhật trong ≤1 tick polling, vì cả hai đọc chung `ModbusRegisterTable`).
  - **Bit 1/Bit 2/Bit 3 là 3 vùng ĐỘC LẬP HOÀN TOÀN — không mirror/đồng nhất giữa chúng (xác nhận lại rõ ràng với người dùng, đảo ngược 1 quyết định sai ở round trước).** Round trước từng cho `WriteOutputAsync`/`TransferHandoverToCommandAsync` tự "mirror" giá trị Bit 2/Bit 3 sang thẳng Bit 1 khi chạy `MockModbusDriver`, với lý do "chưa có PLC thật phản hồi Bit 2 → Bit 1 nên giả lập tạm" — người dùng chỉ rõ đây là hiểu sai bản chất: Bit 1/Bit 3 CHỈ PLC được ghi (PC chỉ đọc), Bit 2 CHỈ PC được ghi (PLC đọc), 3 bit không được phép đồng nhất dù đang test bằng Mock. Đã gỡ bỏ toàn bộ cơ chế mirror. Hệ quả: bấm ON/OFF ở Monitor (ghi Bit 2) **không còn** tự động đổi màu "Điểm" (bind Bit 1) cho các điểm đã nâng cấp `CommandAddress` riêng — muốn test nhanh không cần chờ PLC thật, dùng "Giám sát DATA" ở tab Setup để tự ghi trực tiếp vào Bit 1/Bit 3 mô phỏng PLC báo về. Điểm CHƯA nâng cấp `CommandAddress` (fallback ghi thẳng `Address`) vẫn cập nhật `point.Value` lạc quan ngay lúc ghi — vì với điểm đó không có Bit 1 tách biệt, `Address` chính là bit duy nhất.
- **Không còn giới hạn cứng 16+16 điểm** — mỗi hướng hiển thị nhiều cột song song, mỗi cột tối đa 16 dòng, tự wrap khi vượt; số lượng/địa chỉ điểm thực tế do người dùng tự định nghĩa qua Import CSV (nút "IMPORT LABEL"/"EXPORT LABEL", yêu cầu Admin). Chi tiết đầy đủ về cấu trúc CSV, cơ chế Import/Export, và bẫy kỹ thuật `DataGridColumn.Width` — xem mục 11.
- Cân nhắc thêm hộp thoại xác nhận cho các lệnh điều khiển thủ công có nguy cơ va chạm vật lý (ví dụ ngõ ra xy lanh/hút chân không) — bản cũ chưa có, bản hiện tại cũng **chưa làm**, vẫn còn trong danh sách cải tiến tiếp theo.
- **Output dùng cấu trúc 3-bit (Trạng thái / Lệnh / Bàn giao) thay vì 1 bit chung** — `IoPointDefinition` có thêm `CommandAddress` (Bit 2) và `HandoverAddress` (Bit 3), bên cạnh `Address` gốc (Bit 1):
  - **Bit 1 (`Address`)** — trạng thái đọc, dùng chung cho cả Input lẫn Output, đúng như trước. Điểm mới: `PlcPollingService.PollLoopAsync` giờ **cũng poll `Address` của Outputs** mỗi tick (trước đây chỉ poll Inputs) — để cột "Điểm" của Output phản ánh đúng trạng thái PLC xác nhận, không chỉ là giá trị PC vừa gửi đi.
  - **Bit 2 (`CommandAddress`)** — 2 nút ON/OFF ghi vào đây (không còn ghi thẳng vào `Address`) khi đã cấu hình; tránh PC ghi đè lên bit trạng thái mà PLC có thể đang tự cập nhật liên tục. **Tương thích ngược**: điểm chưa có `CommandAddress` (để trống) → `WriteOutputAsync` fallback ghi thẳng vào `Address` như hành vi cũ, và vẫn cập nhật `point.Value` ngay tại chỗ cho phản hồi UI tức thì. Điểm đã nâng cấp `CommandAddress` riêng: ghi Bit 2 **không** đụng tới `point.Value`/Bit 1 — 2 bit độc lập hoàn toàn (xem ghi chú "3 vùng độc lập" ở trên).
  - **Màu tô 2 nút ON/OFF theo đúng Bit 2, không phải Bit 1** — `IoPointRowViewModel.CommandValue` (property riêng, tách khỏi `Value`) bind vào `Background` của 2 nút (`MonitorTabView.xaml`, converter `IsOnToBrush`/`IsOffToBrush`), trong khi cột "Điểm" tiếp tục bind `Value`/Bit 1 như cũ — 2 chỉ báo màu độc lập cho đúng 2 khái niệm khác nhau (Bit 1 = PLC báo trạng thái thật; Bit 2 = lệnh PC vừa gửi). `CommandValue` được cập nhật lạc quan ngay trong `WriteOutputAsync`/`TransferHandoverToCommandAsync` (hợp lý vì PC là chủ ghi Bit 2, luôn biết chắc giá trị), **cộng thêm** polling riêng mỗi tick trong `PollLoopAsync` (đọc `CommandAddress` qua `ModbusRegisterTable`) để vẫn đồng bộ đúng khi Bit 2 bị đổi từ bên ngoài qua "Giám sát DATA".
  - **Bit 3 (`HandoverAddress`)** — trạng thái PLC đang tự quản lý lần cuối. `MonitorTabViewModel.SetModeAsync` gọi `PlcPollingService.TransferHandoverToCommandAsync(Outputs)` **đúng 1 lần** khi chuyển Auto→Manual: đọc Bit 3, ghi đè vào Bit 2 — kỹ thuật **"bumpless transfer"** chuẩn công nghiệp, tránh ngõ ra nhảy trạng thái đột ngột khi giao quyền điều khiển cho operator. Không lặp lại/theo dõi tiếp sau đó. Chỉ đọc Bit 3/ghi Bit 2 — không đụng Bit 1.
  - CSV Import/Export (`CsvIoLabelService`) mở rộng thêm 2 cột `CommandAddress,HandoverAddress` ở cuối header — chỉ có ý nghĩa với Output; Input có điền vẫn Import được nhưng bị **cảnh báo + tự xóa** (không lỗi chặn, để không phá hỏng cả file chỉ vì 1 dòng điền nhầm).
  - Đây là điểm cần lưu ý khi cấu hình địa chỉ Bit 1 cho Output: vì PC cần **đọc** Bit 1 (dữ liệu chiều PLC→PC), địa chỉ đó nên nằm trong **Khối Input** đã cấu hình ở Setup (dù bản thân điểm I/O là "Output") — còn Bit 2 (Bit lệnh, PC ghi) nên nằm trong **Khối Output**. Đừng nhầm "hướng của điểm I/O" với "khối polling" — 2 khái niệm độc lập.

### Tab 3 — Cài đặt/Giới thiệu (Setup)
Chưa được làm ở bản cũ — xây dựng mới hoàn toàn. **Đã cài đặt (round: khóa Modbus RTU + chuẩn hóa combobox + khối thanh ghi)**:
- **Protocol tạm khóa cứng Modbus RTU** — ô hiển thị chỉ đọc (`SetupTabViewModel.Protocol => ProtocolType.ModbusRtu`, không bind 2 chiều), tooltip ghi rõ "sẽ mở khóa khi hỗ trợ thêm giao thức khác". Enum `ProtocolType` (MC/SLMP/CC-Link/EtherNet-IP) vẫn còn trong `EolTester.Configuration.Models` để dành cho sau, chỉ chưa cho chọn qua UI.
- **PC có thể làm Master hoặc Slave, chuyển đổi được qua `ComboBox` "Vai trò"** — **đã đảo ngược** quyết định trước đó ("PC luôn Master, loại hẳn Slave"): quản lý xác nhận PLC thực tế sẽ làm Master quản lý nhiều thiết bị RS-485 khác, PC phải là 1 Slave trong số đó. Chi tiết kiến trúc/lý do đầy đủ xem mục 6 "PC làm Modbus Slave" và docs/session-log/history.md mục 15.
- **Tham số serial đã chuẩn hóa thành ComboBox** thay vì TextBox tự do: Cổng COM (`SerialPort.GetPortNames()`, có nút "Làm mới" quét lại danh sách — cần gói NuGet `System.IO.Ports`, không có sẵn trong `net10.0-windows` mặc định), Baud rate (danh sách chuẩn `4800/9600/19200/38400/57600/115200`), Parity (enum `ModbusParity { None, Odd, Even }`, không còn là `string` tự do), Data bits (`7/8`), Stop bits (`1/2`). Địa chỉ trạm (Slave ID), chu kỳ polling, timeout/retry vẫn là ô nhập số.
- **Khối thanh ghi đọc/ghi liên tục** (`ConnectionSettings.InputBlock`/`OutputBlock`, kiểu `ModbusRegisterBlock { StartAddress, RegisterCount }`) — mỗi chu kỳ polling PC đọc 1 lần toàn bộ khối Input và **giờ đã thật sự ghi 1 lần toàn bộ khối Output** (trước đây chỉ có ý định thiết kế, chưa triển khai — `PlcPollingService.PollLoopAsync` nay có thêm vòng `for` ghi từng thanh ghi trong `_outputBlockStart.._outputBlockStart+_outputBlockCount` lấy giá trị từ `ModbusRegisterTable` mỗi tick, đặt ngay sau vòng đọc Input Block), thay vì đọc/ghi từng điểm I/O riêng lẻ (hiệu quả hơn nhiều so với N request cho N điểm). Vòng ghi theo chu kỳ này **không thay thế** ghi theo sự kiện (ON/OFF, Reset, Xác nhận NG...) — cả 2 chạy song song, ghi theo chu kỳ chỉ "làm mới/giữ" lại đúng giá trị đang có trong bộ nhớ đệm, không có nguồn dữ liệu nào khác nên không thể ghi sai. **Địa chỉ ghi theo sự kiện độc lập với phạm vi Khối Output đã khai báo** — có thể trùng hoặc nằm ngoài hoàn toàn, việc ghi driver vẫn luôn xảy ra (chỉ khác: nếu ngoài cả 2 khối, bộ nhớ đệm cục bộ không giữ lại giá trị đó, "Giám sát DATA" sẽ không đọc lại được, và vòng ghi theo chu kỳ sẽ không tự làm mới riêng địa chỉ đó — đã xác nhận với người dùng đây đúng là hành vi mong muốn). **Cố ý tách biệt** "không gian địa chỉ khai báo" (có thể lớn, VD D0000–D2000, không tốn gì về bộ nhớ PC) khỏi "kích thước khối polling thực tế mỗi chu kỳ" (nên nhỏ, chỉ đúng số thanh ghi I/O cần dùng) — vì Modbus RTU giới hạn cứng tối đa **125 thanh ghi/lần đọc (FC03)** và **~123/lần ghi (FC16)**; khối lớn hơn cần nhiều request nối tiếp, dễ vượt chu kỳ polling. UI chỉ **cảnh báo** (không chặn) khi `RegisterCount > 125`, kèm số request ước tính (`ceil(count/125)`) — vì driver thật (tương lai) có thể tự chia nhỏ request nếu cần.
- **Địa chỉ mức bit kiểu Mitsubishi `D1000.1`** — `ModbusWordAddress` (`EolTester.Communication/ModbusWordAddress.cs`, record struct `{ WordAddress, BitIndex? }`) parse cả `"D1000"` (cả từ 16-bit, dùng cho giá trị analog/register) lẫn `"D1000.1"` (1 bit trong từ, dùng cho I/O digital, bit 0-15). Modbus tự nó không có lệnh đọc/ghi 1 bit riêng — driver thật (tương lai) phải đọc/ghi cả từ rồi tự AND/shift để tách/gộp bit; ghi 1 bit lẻ cần chiến lược tránh đè bit khác (giữ bản sao từ trong bộ nhớ nếu PC là chủ sở hữu duy nhất của từ đó, hoặc read-modify-write nếu từ dùng chung với PLC) — quyết định theo từng thanh ghi cụ thể khi có bảng địa chỉ PLC thật, chưa tự suy đoán chung chung. `IoPointDefinition.Address` (chuỗi hiện có trong `io-map.json`) tiếp tục là nguồn nhập liệu, không đổi cấu trúc CSV.
- **`ModbusRegisterTable` — "biến D1005" trong bộ nhớ PC** (`EolTester.Communication/ModbusRegisterTable.cs`) — bảng thanh ghi in-memory (`Dictionary<int, ushort>`), truy cập bằng đúng tên PLC dùng qua `TryGetValue("D1005", out value)` (cả từ) hoặc `TryGetValue("D1005.1", out value)` (1 bit, tận dụng `ModbusWordAddress.Parse`). **Chỉ đọc/ghi được trong đúng phạm vi đã cấu hình ở tab Setup** (`InputBlock`/`OutputBlock`) — đây là ràng buộc nghiệp vụ chủ động (không phải giới hạn Modbus), tránh app lỡ đọc/ghi nhầm sang vùng D không thuộc quyền quản lý của nó. `ConfigureAllowedRanges(...)` được gọi lại mỗi khi `SetupTabViewModel.LoadAsync`/`SaveAsync` chạy (kể cả sau khi Admin đổi cấu hình, không cần khởi động lại app). **ĐÃ SỬA bug thật: trước đây `_words.Clear()` vô điều kiện mỗi lần gọi** — hệ quả nghiêm trọng là mỗi lần Lưu cấu hình Setup (kể cả không đổi range) hoặc mỗi lần mất-kết nối/reconnect (`RestartConnectionAsync` gọi lại `ApplyRegisterTableRanges`) xóa sạch luôn cache D100-D199 (thanh ghi PC ghi cho PLC đọc) dù các địa chỉ đó vẫn còn hợp lệ trong range mới — dữ liệu output đang giữ bị mất trắng. Đã đổi sang **chỉ xóa các key thật sự nằm ngoài range mới** (`_words.Keys.Where(addr => !CanReadAddress(addr) && !CanWriteAddress(addr))`), giữ nguyên toàn bộ giá trị hợp lệ. Đăng ký Singleton trong DI (`App.xaml.cs`) — 1 bảng dùng chung toàn app.
- **2 dải địa chỉ cố định D0-D99 (chỉ-đọc) / D100-D199 (chỉ-ghi), độc lập với Input/Output Block cấu hình được (chỉ cho phép trong khoảng D200-D2000)** — theo yêu cầu người dùng: PLC quản lý 2 dải D thấp riêng cho mục đích trao đổi dữ liệu nhanh/cố định (không cần khai báo qua UI Setup), tách biệt hẳn khỏi Input/Output Block vốn linh hoạt cấu hình theo model. `ModbusRegisterTable.CanReadAddress`/`CanWriteAddress` (Role=Master) áp cứng quy tắc: D0-D99 luôn đọc được, không bao giờ ghi được; D100-D199 luôn ghi được, không bao giờ đọc được (đọc ở đây nghĩa là đọc **thật** từ PLC — đọc lại giá trị đã cache trong bộ nhớ qua `TryGetWord`/`TryGetValue` vẫn được phép cho cả 2 dải, vì đó chỉ là xem lại giá trị PC vừa ghi, không phải hỏi lại PLC); D≥200 chỉ đọc/ghi được nếu nằm trong khoảng D200-D2000 đã đăng ký. **`SetupTabViewModel.SaveAsync` chặn lưu nếu `InputBlockStartAddress`/`OutputBlockStartAddress` nằm ngoài D200-D2000** (hằng số `MinConfigurableBlockAddress`/`MaxConfigurableBlockAddress`), báo lỗi qua `StatusMessage` (xem "1 dòng thông báo duy nhất" bên dưới). `PlcPollingService.RefreshRuntimeStateAsync` (2 phase mỗi tick ~500ms — đọc D0-D99 + Input Block, rồi ghi D100-D199 + Output Block) có thêm guard `CanReadAddress`/`CanWriteAddress` ngay trước khi gọi driver cho phần Input/Output Block, để 1 file `connection.json` cũ lỡ lưu Start Address ngoài phạm vi (từ trước khi có validation này) không khiến app **ghi vật lý** đè lên dải D0-D99 chỉ-đọc.
  - **`ApplyRegisterTableRanges()` đăng ký NGUYÊN 1 khối rộng D200-D2000 vào `ModbusRegisterTable.ConfigureAllowedRanges`** (không phải 2 range hẹp trùng khớp `InputBlock`/`OutputBlock` như thiết kế ban đầu) — quyết định đổi vì "Giám sát DATA" (xem dưới) cần đọc/ghi được BẤT KỲ địa chỉ nào trong D200-D2000, không riêng cửa sổ đang polling. Không ảnh hưởng vòng lặp polling vật lý (`RefreshRuntimeStateAsync`), vì vòng đó tự giới hạn theo `_inputBlockStart/_inputBlockCount`/`_outputBlockStart/_outputBlockCount` riêng, độc lập với allowed-range của `ModbusRegisterTable`.
  - **1 dòng thông báo duy nhất (`SetupTabViewModel.StatusMessage` + `StatusMessageIsError`)** dùng chung cho cả 3 nguồn: kết quả Test Connection (xanh lá/đỏ), lỗi validate khi Lưu (đỏ), và xác nhận Lưu thành công (`Setup_SaveSuccess`, xanh lá — mới thêm, trước đây bấm Lưu thành công không có phản hồi gì). Cố ý **không** dùng 3 property/TextBlock riêng như thiết kế ban đầu (`TestConnectionResult`, `RegisterWatchValidationSummary`, `SaveResultMessage`) — 3 dòng độc lập có thể cùng hiển thị đồng thời (VD Test Connection xong rồi Lưu thành công ngay sau đó, dòng Test Connection cũ không tự mất), khiến panel cao thêm bất ngờ, có lúc tràn thành thanh cuộn dọc và làm khuất mất đoạn cuối 1 thông báo — lỗi thật đã gặp, xem docs/session-log/history.md mục kế tiếp. Đặt ở cột trái, ngay dưới nút "Lưu cấu hình"/"Test Connection" — **không đặt ở panel "Giám sát DATA" bên phải**, vì panel đó là lưới cố định 30 ô gần lấp đầy chiều cao khung canvas cố định, dễ tràn/mất chỗ hiển thị.
  - **Đã nối vào vòng lặp polling thật (Master)** — `PlcPollingService.PollLoopAsync` mỗi tick đọc **toàn bộ `InputBlock`** đã cấu hình bằng 1 request khối (`ReadRegistersAsync`, tự chia nhỏ nếu vượt 125 thanh ghi — xem mục "Lệnh thường dùng"/lịch sử phiên làm việc, đã đổi từ vòng lặp N request 1-thanh-ghi/lần vì quá chậm) rồi lưu vào `PlcRegisterImage`, thay vì trước đây chỉ đọc các từ được `Inputs` (io-map.json) tham chiếu. Nhờ vậy mọi thanh ghi trong phạm vi Input Block đều có giá trị mới nhất — kể cả thanh ghi chưa gắn `IoPointDefinition` nào (dùng cho "Giám sát thanh ghi" bên dưới). `PlcPollingService.SetInputBlockRange(...)` được `SetupTabViewModel.SaveAsync` gọi lại mỗi khi lưu cấu hình mới, có hiệu lực ngay không cần khởi động lại app.
  - **Test Connection tự dừng kết nối Master đang chạy trước khi test, và khởi động lại sau đó** — bug thật đã gặp: khi Role=Master đang polling live (giữ cổng COM mở độc quyền), bấm "Test Connection" luôn báo lỗi "Access to the path 'COMx' is denied" vì driver test cố mở cùng cổng COM đang bị `PlcPollingService` giữ. Đã sửa `SetupTabViewModel.TestConnectionAsync`: bọc quanh `TestConnectionCoreAsync` bằng `PlcPollingService.PauseAsync()` (hủy vòng lặp polling + ngắt driver hiện tại) trước khi test, rồi `try/finally` đảm bảo `RestartConnectionAsync()`/`ConnectAndPollAsync()` chạy lại sau khi test xong (dù test thành công hay lỗi) — không để app "quên" kết nối lại. Cùng lúc, `PlcPollingService.RestartConnectionAsync` được refactor tách thành `PauseAsync()` (public, dùng chung) + `ConnectAndPollAsync()`, và cả `SaveAsync`/`TestConnectionAsync` trong `SetupTabViewModel` đều có guard `_connectionOperationInProgress` (chặn tái nhập khi người dùng bấm liên tiếp 2 nút trong lúc 1 thao tác đang chạy — tránh race 2 vòng polling/2 lần mở cổng COM chồng nhau).
- **"Giám sát DATA"** — khối bên phải tab Setup, theo đề xuất người dùng (`docs/legacy-ui/Setting paged V2.png`), tận dụng khoảng trống bên phải vốn không dùng tới khi nội dung cột trái bị dồn dọc gây che nút Lưu/Test Connection (xem "Sửa lỗi layout" dưới). **Đây là màn hình đưa cho khách hàng xem** — sau khi thử bản đầu (thêm dòng kiểu bảng tính + nút "Ghi" + nút xóa + đoạn text hướng dẫn dưới tiêu đề), người dùng yêu cầu thiết kế lại gọn hơn, không có vẻ "kỹ thuật/dev-facing":
  - **Lưới cố định 30 ô** (`SetupTabViewModel.RegisterWatchColumn1`/`RegisterWatchColumn2`, mỗi cột đúng 15 dòng — hằng số `RegisterWatchRowsPerColumn`, tạo 1 lần trong constructor) — **không** phải danh sách thêm/xóa động: không có dòng trống cuối bảng kiểu bảng tính, không nút "Thêm dòng", không nút xóa. Người dùng gõ địa chỉ `Dxxxx` trực tiếp vào ô Address của bất kỳ ô nào trong 30 ô cố định.
  - **Địa chỉ hợp lệ = bất kỳ đâu trong D0-D2000 (`SetupTabViewModel.IsWithinAppOwnedRange`)** — rộng hơn hẳn Input/Output Block Start Address (chỉ D200-D2000), vì "Giám sát DATA" cho phép theo dõi/ghi cả dải D0-D99/D100-D199 cố định của Master mode, không chỉ cửa sổ Input/Output Block đang polling. Địa chỉ ngoài cửa sổ Input/Output Block **không tự làm mới theo chu kỳ** (chỉ hiển thị đúng giá trị Admin vừa tự gõ — echo) — RIÊNG D0-D99 vẫn được polling thật mỗi tick (Phase 1 cố định), nên luôn hiển thị giá trị PLC thời gian thực.
  - **Admin ghi tay được vào BẤT KỲ địa chỉ nào trong D0-D2000, kể cả D0-D99 (chỉ-đọc theo quy tắc cứng)** — quyết định có chủ đích của người dùng: không chặn ghi ở tầng UI dù D0-D99 vốn là PLC-owned, chấp nhận đánh đổi "nếu đang polling thật thì giá trị Admin gõ vào D0-D99 sẽ bị lần đọc thật tiếp theo (~500ms) ghi đè lại" — dùng để test/theo dõi tạm thời, không phải ghi đè vĩnh viễn. `ModbusRegisterTable.TryUpdateWord`/`TrySetWordForMasterWrite` vẫn tự chặn ở tầng cache theo `CanWriteAddress` (D0-99 luôn `false`) — ghi cache cho D0-99 lặng lẽ no-op, nhưng lệnh ghi vật lý qua `PlcPollingService.WriteRawWordAsync` vẫn được gửi thật xuống driver không điều kiện, đúng ý người dùng.
  - **Sửa giá trị tại chỗ** — cột "Giá trị" vừa hiển thị realtime (timer ~500ms) vừa nhận input ghi trực tiếp: gõ giá trị mới, Enter/rời ô là tự ghi xuống thanh ghi, không cần nút "Ghi" riêng. Cơ chế phân biệt "timer tự cập nhật" với "người dùng vừa gõ xong": `RegisterWatchRowViewModel.SetDisplayFromPoll(...)` (timer gọi, đặt cờ suppress) vs. `partial void OnCurrentValueDisplayChanged` raise event `ValueEditedByUser` (chỉ khi không suppress — tức WPF tự commit binding `TextBox.Text` mặc định `UpdateSourceTrigger=LostFocus`, đúng ý "gõ xong rồi Enter/rời ô").
  - **`RegisterDisplayFormat` có 6 giá trị: Bin, Hex, Dec, Decimal, Float, Char** (đổi từ 2 giá trị Decimal/Hex ban đầu) — độc lập với `RegisterDataType` (WordSigned/WordUnsigned/DwordSigned/DwordUnsigned/Float32, vẫn quyết định đọc 1 hay 2 thanh ghi + giá trị số gốc signed/unsigned). Bin/Hex/Dec/Decimal hiển thị đúng giá trị số đó (Decimal = chia cố định 100, hiển thị 2 chữ số thập phân — không có cột "số chữ số thập phân" riêng, giữ tinh thần không thêm cột phụ); Float/Char **bit-cast lại cùng bit pattern** thành số thực IEEE-754 hoặc ký tự ASCII (2 ký tự cho Word 16-bit, 4 ký tự cho Dword/Float32 32-bit) — hữu ích để "xem thử" 1 thanh ghi dưới nhiều góc nhìn khi chưa rõ PLC lưu kiểu gì.
  - **Ghi giá trị hữu ích nhất với thanh ghi Output** — verify qua UI thật: ghi vào 1 địa chỉ trong Khối Input bị vòng lặp polling (đọc lại toàn bộ Input Block mỗi 500ms) ghi đè gần như ngay lập tức (đúng thiết kế — Input phản ánh giá trị PLC báo về, PC không nên tự quyết); ghi vào địa chỉ trong Khối Output thì giá trị "settle" ổn định đúng như đã ghi (không có gì tự đọc/ghi đè khối Output ngoài chính người dùng) — đúng đề xuất ban đầu "hữu ích với các thanh ghi Output". Ở Role=Master: cập nhật `ModbusRegisterTable` + đẩy ra PLC qua `PlcPollingService.WriteRawWordAsync`. Ở Role=Slave: chỉ cập nhật `ModbusRegisterTable` cục bộ (PC không có kết nối Master chủ động) — đúng giá trị PLC sẽ đọc được ở lần hỏi tiếp theo. Mọi lần ghi đều ghi audit log qua `IAuditLogService` (tương tự lệnh điều khiển thủ công ở tab Monitor).
  - **Lưu/nạp**: `register-watch.json` (`JsonRegisterWatchStore`) lưu tối đa 24 dòng, mỗi dòng có `Order` = đúng vị trí ô (0-23) — nạp lại (`RegisterWatchRowViewModel.ApplyDefinition`) áp đúng vào ô cố định theo `Order`, không tạo/xóa instance — **verify qua UI thật**: điền ô ở vị trí cột 2 dòng 5 (index 16), Lưu, khởi động lại app, ô vẫn đúng vị trí cũ. Dùng chung nút "Lưu cấu hình" hiện có, không có nút Save riêng cho khối này.
  - **Yêu cầu Admin** như toàn bộ tab Setup, không có tầng quyền riêng (đã hỏi lại người dùng, chọn phương án đơn giản nhất).
  - **Đoạn text hướng dẫn (định dạng `Dxxxx`, giả định word thấp trước cho Dword/Float, phạm vi Input/Output Block) đã bị gỡ khỏi UI** theo yêu cầu người dùng (màn hình khách hàng, khó thiết kế đẹp với nhiều chữ giải thích) — nội dung được giữ lại **ở đây** (mục 7) để tự nhắc khi bắt đầu dự án mới hoặc khi được hỏi tới:
    - Định dạng địa chỉ bắt buộc `Dxxxx` (không có hậu tố bit — khác với địa chỉ I/O ở tab Monitor vốn cho phép `Dxxxx.b`).
    - Dword/Float32 đọc/ghi 2 thanh ghi liên tiếp theo thứ tự **word thấp trước** — vẫn là **giả định chưa xác nhận với PLC thật** (cùng nhóm giả định với thứ tự word khi PC làm Slave, xem mục 13).
    - Địa chỉ phải nằm trong Khối Input/Output đã cấu hình ở khối trái — ghi ngoài phạm vi bị chặn (báo lỗi qua tooltip dòng, không lưu được/không ghi được).
- **Sửa lỗi layout che nút Lưu/Test Connection khi Role=Master** — nguyên nhân: toàn bộ nội dung Setup từng nằm trong 1 `StackPanel` cột dọc duy nhất (`MaxWidth="560"`), trong khi vùng nội dung tab có chiều cao cố định (canvas `1400×765`, mục 12); khi Master có thêm khối Register Block nên nội dung dài hơn Slave, vượt khung nhìn, tràn xuống bị Footer che mất — đúng lúc bên phải màn hình lại đang bỏ trống gần 60% bề ngang. Khắc phục: đổi container gốc thành `Grid` 2 cột — cột trái (nội dung cũ, bọc thêm `ScrollViewer` làm lớp phòng vệ nếu sau này dài thêm) cạnh cột phải (khối "Giám sát DATA" ở trên) — vừa hết che nút, vừa tận dụng không gian trống thay vì chỉ vá bằng thanh cuộn. **Bẫy layout gặp phải**: đặt 2 `DataGrid` cạnh nhau trong `StackPanel Orientation="Horizontal"` khiến cột cuối cùng khai báo `Width="*"` bị co gần như bằng 0 (StackPanel không stretch con theo chiều ngang) — phải đổi sang `Grid` 2 `ColumnDefinition Width="*"` để mỗi `DataGrid` có bề rộng thật để cột `*` bên trong giãn đúng.
- **`ModbusRtuDriver` thật đã viết xong và đã kiểm chứng với PLC thật** (`src/EolTester.Communication/ModbusRtu/ModbusRtuDriver.cs`, NModbus + `SerialPort`, PC làm Master) — kết nối thành công lần đầu qua COM4/19200/None/8/1, Slave ID 2, chỉ báo trạng thái hiện đúng "Đã kết nối" với dữ liệu ổn định đọc thật từ PLC. **Là driver DUY NHẤT khi Role=Master — đã bỏ hẳn dropdown "Driver" (Mock/Real) và `MockModbusDriver`** (rủi ro vận hành nếu ai đó vô tình để chế độ giả lập trên máy thật, xem mục 4). `ConnectionState.Connected` **chỉ** được xác nhận sau ít nhất 1 lần đọc/ghi Modbus thật sự thành công (không phải chỉ mở được cổng COM) — tránh hiển thị sai khi cổng COM tồn tại nhưng không có PLC phản hồi. `PollLoopAsync` có thêm catch-all cho mọi exception từ driver thật (timeout/mất kết nối) để 1 lỗi không giết chết hẳn vòng lặp polling nền — tick kế tiếp (~500ms) tự đóng vai trò retry. Đổi ComPort/Baud/.../ở Setup có hiệu lực ngay khi bấm "Lưu cấu hình" qua `PlcPollingService.RestartConnectionAsync()`, không cần khởi động lại app (chỉ Role Master/Slave vẫn cần restart).
  - **Test lần đầu với PLC thật đọc được toàn giá trị 0** (D1000, D1003, D1010, D1106) — kết nối/giao thức xác nhận hoạt động đúng (ổn định, không phải Mock), nhưng **chưa xác nhận được các địa chỉ D này có đúng là nơi PLC lưu dữ liệu thật hay không** — có thể do quy ước offset địa chỉ Modbus (0-based/1-based) hoặc bản đồ D-register→Modbus register của gateway/PLC thật khác giả định hiện tại. Đây là câu hỏi mở cần đối chiếu với tài liệu/kỹ sư PLC, không phải bug code (xem mục 13).
  - Interface `IPlcCommunicationDriver` vẫn chỉ đọc/ghi từng thanh ghi lẻ (chưa có method đọc/ghi nguyên khối) — driver thật hiện gọi 1 request Modbus/thanh ghi giống hệt Mock, đủ dùng cho khối nhỏ hiện tại nhưng sẽ chậm nếu khối lớn (xem mục 13, cảnh báo >125 thanh ghi/request).
  - **Bẫy đã gặp: app crash câm lặng ngay lúc khởi động khi `connection.json` trỏ Driver=Real vào 1 cổng COM không tồn tại trên máy đang chạy** (VD copy `connection.json` từ máy đã đấu PLC thật sang máy dev không có PLC) — `PlcPollingService.StartAsync` đã có try/catch quanh `_driver.ConnectAsync(ct)` (không chặn khởi động khi cổng COM lỗi), nhưng dòng `await RefreshRuntimeStateAsync(...)` gọi NGAY SAU ĐÓ để nạp dữ liệu sớm (ưu tiên hơn chờ tick đầu ~500ms của `PollLoopAsync`) lại nằm NGOÀI try/catch đó — bên trong nó gọi lại `EnsureConnectedAsync`/`ConnectAsync` và ném `FileNotFoundException`/`IOException` lần 2, bay thẳng lên `App.OnStartup` và crash cả app **trước khi `MainWindow` kịp `Show()`** — không có dialog lỗi, không có cửa sổ nào hiện ra, `Start-Process` từ PowerShell không thấy gì (đúng bẫy đã ghi ở mục 9 cho License, hóa ra áp dụng cho bất kỳ exception nào ném ra trong `App.OnStartup` trước khi tạo `MainWindow`, không riêng license). **Chỉ phát hiện được bằng cách chạy `dotnet run` ở foreground** (không phải `Start-Process`/double-click) để thấy full stack trace. Đã bọc thêm try/catch (giống hệt tinh thần catch-all của `PollLoopAsync`) quanh lời gọi `RefreshRuntimeStateAsync` này trong `StartAsync`.
  - **ĐÃ GIẢM THIỂU TỔNG THỂ (2026-08-28):** `App.OnStartup` tách toàn bộ phần thân thành `RunStartupSequenceAsync()` bọc trong 1 `try/catch`; bất kỳ exception nào ném ra TRƯỚC `mainWindow.Show()` (connection.json hỏng, cổng COM không tồn tại, `Protocol` chưa hỗ trợ, lỗi DI, `SerialPort.GetPortNames()` ném...) → `HandleFatalStartupError`: ghi log + `MessageBox` song ngữ nêu `{Type}: {Message}` + đường dẫn log, rồi `Shutdown()` có kiểm soát. Không còn "tắt không dấu vết". Cố ý giữ "fail loud" — KHÔNG tự chạy tiếp bằng cấu hình mặc định khi file cấu hình hỏng (máy công nghiệp offline chạy nhầm cổng COM/nhầm Role còn nguy hiểm hơn). Các try/catch cục bộ cũ (quanh `_driver.ConnectAsync`, `RefreshRuntimeStateAsync`, slave `StartAsync`...) vẫn giữ nguyên — lớp ngoài này chỉ là lưới cuối cho những trường hợp chưa lường hết.
- **Bẫy đã gặp khi đổi `Parity` từ `string` sang `enum`**: `System.Text.Json` mặc định serialize enum thành **số**, không phải tên chuỗi — file `connection.json` cũ (lưu từ khi `Parity` còn là `string`, VD `"None"`) không tự đọc lại được sau khi đổi kiểu, ném `JsonException` ngay lúc khởi động app (crash trước khi `MainWindow` hiện ra, `Start-Process` không thấy exception — phải `dotnet run` foreground mới thấy). Đã thêm `Converters = { new JsonStringEnumConverter() }` vào `JsonConnectionSettingsStore` — vừa sửa lỗi đọc file cũ, vừa khiến enum lưu dạng tên chữ dễ đọc (`"Protocol": "ModbusRtu"` thay vì `"Protocol": 0`), đúng tinh thần "JSON dễ đọc/sửa tay" ở mục 2. Áp dụng luôn cho mọi enum khác trong `ConnectionSettings` (không chỉ `Parity`).
- Về sau: thông tin máy/model, phiên bản phần mềm, khối "Giới thiệu" (about).
- Việc thay đổi ở tab này phải yêu cầu vai trò **Admin**, tương tự tab Setting.
- **Layout cột trái "Cấu hình kết nối PLC" đã được thiết kế lại theo mockup `docs/legacy-ui/Setting paged V3.png`** — gộp cặp trường liên quan vào chung 1 hàng thay vì mỗi trường 1 hàng riêng: "Vai trò / Driver" (2 `ComboBox` cạnh nhau, width 140 mỗi cái) và "Baud rate / Parity" (tương tự) — giảm từ 11 xuống còn 8 hàng cấu hình. Cột nhãn thu hẹp từ 180 xuống 150px cho khớp tỉ lệ mockup. Dòng hint text màu xám giải thích "Khối thanh ghi đọc/ghi..." (`Setup_RegisterBlockHint`) đã bị gỡ khỏi UI (cùng tinh thần "màn hình khách hàng, không đưa nhiều chữ giải thích" đã áp dụng cho "Giám sát DATA" — resx key vẫn còn nhưng không dùng). `StationAddress` (Master) và `MySlaveId` (Slave) đặt chung 1 `Grid.Row` với `Visibility` loại trừ nhau (trước đó ở 2 hàng riêng) — đơn giản hơn, không để lại khoảng trống khi đổi Role. Verify UI thật xác nhận khớp mockup ~90-95%.

### Tab 4 — Set spec. (Setting)
- Cùng bảng bước kiểm tra như tab Main nhưng ở chế độ chỉnh sửa được (không có cột Giá trị) — bind cả hai vào cùng một model `TestStepDefinition` để chỉ có một nguồn dữ liệu duy nhất, không phải hai bảng đồng bộ tay với nhau.
- Chỉ chỉnh sửa được sau khi đăng nhập; "XÁC NHẬN THAY ĐỔI" thực thi thay đổi và bắt buộc ghi một dòng audit với giá trị cũ → mới, người dùng, thời điểm.
- **Giá trị Giới hạn dưới/trên tách làm 2 lớp**: `TestStepRowViewModel.EditLowerLimit`/`EditUpperLimit` (đang gõ, bind vào DataGrid Set Spec.) độc lập với `LowerLimit`/`UpperLimit` (đang hiệu lực, dùng ở tab Main để hiển thị + tính Đạt/Không đạt). Gõ ở Set Spec. KHÔNG làm tab Main "nhảy số" ngay — chỉ khi bấm "Xác nhận thay đổi" (`SettingTabViewModel.ConfirmChangesAsync`) mới chốt Edit* → LowerLimit/UpperLimit (đồng thời ghi Definition + lưu file + đẩy PLC). Nếu giá trị đang gõ khác giá trị hiện có trong thanh ghi PLC tương ứng (`RegisterLowerLimit`/`RegisterUpperLimit`, đọc live từ `ModbusRegisterTable` qua LowerLimitAddress/UpperLimitAddress mỗi tick polling) thì ô đó tô nền cam nhạt + chữ đỏ đậm (`IsLowerLimitPending`/`IsUpperLimitPending`) để cảnh báo còn thay đổi chưa xác nhận/chưa đẩy xuống.
- **Bẫy WPF đã gặp — DataGrid không edit được khi ItemsSource là IEnumerable lazy**: `EditableHighModeSteps`/`EditableLowModeSteps` từng là `IEnumerable<T>` (LINQ `.Where` trực tiếp) — WPF tạo `CollectionView` mặc định cho nguồn `IEnumerable`-only, loại này KHÔNG hỗ trợ `IEditableCollectionView`, khiến `BeginEdit` (double-click/F2 vào ô) ném `InvalidOperationException` ở MỌI lần thử, bị `DispatcherUnhandledException` nuốt âm thầm — không crash nhưng ô không bao giờ vào được chế độ edit thật sự (không có triệu chứng lỗi nào hiện ra ngoài, chỉ thấy trong log). Đã sửa bằng cách đổi kiểu trả về sang `List<TestStepRowViewModel>` (IList, `.ToList()`) — bài học: bất kỳ `DataGrid`/`ListView` nào cần cho phép sửa cell trực tiếp, `ItemsSource` phải là `IList` (hoặc `ObservableCollection`), không được để nguyên `IEnumerable` từ LINQ.
- **Bẫy Slave mode — lệnh ghi PLC không có tác dụng gì khi Role=Slave**: `PlcPollingService.WriteBitAsync`/`WriteWordToAddressAsync` (dùng cho Start/Stop/Reset/Xác nhận NG/CMD_MODE/tham số Set Spec/giới hạn High-Low) trước đây LUÔN gọi thẳng driver vật lý bất kể Role — ở Slave, driver chưa từng kết nối (không có `ConnectAndPollAsync`/`PollLoopAsync`), nên mọi lệnh ném exception (bị nuốt) và **cache (`ModbusRegisterTable`) cũng không được cập nhật theo** — từ góc nhìn người dùng, các nút bấm/nút Xác nhận "không có tác dụng gì". Đã sửa: luôn cập nhật cache trước (PC là Slave vẫn cần giữ đúng giá trị để `ModbusSlaveService` trả lời PLC Master thật hỏi tới), chỉ gửi vật lý xuống driver khi đang chủ động làm Master (`PlcPollingService.IsActiveMaster`). **Đã rà soát xác nhận: mọi entry point ghi PLC hiện có (Start/Stop/Reset/Xác nhận NG/đổi Auto-Manual/điều khiển thủ công Output/Xác nhận thay đổi Set Spec/auto-save tham số/Giám sát DATA) đều đi qua `WriteBitAsync`/`WriteWordToAddressAsync`/`WriteRawWordAsync` (3 hàm đã fix cache-first) — Role=Slave vận hành đúng cho toàn bộ nút/giá trị hiện có, với điều kiện mọi địa chỉ khai báo đều đúng định dạng `Dxxxx`/`Dxxxx.b`** (địa chỉ discrete kiểu cũ, không phải Dxxxx, chỉ hoạt động khi Role=Master — không có đường cache cho định dạng này, cần tránh dùng cho điểm I/O/tham số mới).
- **"Xác nhận thay đổi" bị CHẶN nếu Giới hạn dưới > Giới hạn trên** (`TestStepRowViewModel.IsLimitRangeInvalid`, so `EditLowerLimit`/`EditUpperLimit`) — dữ liệu vô lý không thể dùng để so sánh Đạt/Không đạt lúc vận hành. Ô vi phạm tô đỏ đậm (nền `#D32F2F`, chữ trắng, ưu tiên cao hơn màu cam "pending") ngay khi rời ô; bấm "Xác nhận thay đổi" trong lúc còn vi phạm sẽ KHÔNG lưu file/KHÔNG ghi PLC/KHÔNG audit log, chỉ hiện `StatusMessage` màu đỏ (`StatusMessageIsError`, cùng pattern với `SetupTabViewModel`) liệt kê tên các bước vi phạm.
- Khối Cài đặt (độ dài mã scan, thời gian test High/Low, thời gian nhấn giữ nút...) — mô hình hóa thành một object `TestParameters` có kiểu rõ ràng, không phải các trường rời rạc, để thêm một tham số mới chỉ là thay đổi model + tự sinh dòng hiển thị, không phải hardcode thêm UI.
- **Bảng "Cài đặt thời gian (x0.1s)" / "Timer Setting (x0.1s)" (cột giữa tab Set Spec.) gồm 13 dòng: 3 thời gian cố định (test High/Low mode, giữ nút — nhãn đã bỏ hậu tố "(ms)") xếp ĐẦU bảng, rồi tới 10 "Delay timer" tùy chỉnh** — **đơn vị mọi ô là x0.1s (PLC), KHÔNG phải ms** (tên field/khóa `PARAM_*_MS`/`DelayTimersMs` giữ hậu tố "Ms" vì lịch sử, chỉ nhãn UI + đơn vị đổi). Mặc định khi build: High/Low = `20` (2.0s), giữ nút = `5` (0.5s), 10 Delay timer = `5` (0.5s). — trước đây 3 thời gian cố định là 3 `TextBox` riêng ở cột "Cài đặt tham số" (phải), đã gộp hết vào 1 `DataGrid` duy nhất (`SettingTabViewModel.TimerSettingRows`, `List<TimerSettingRowViewModel>`) theo yêu cầu người dùng. Mỗi dòng có `LabelKey` (resx) + `ParamKey` (khóa PLC) — danh sách 13 cặp cố định trong `TimerSettingRowViewModel.Definitions`, dùng chung giữa `SettingTabViewModel.LoadParametersAsync` (xây dựng dòng) và `App.xaml.cs` (ghi đè nhãn resx). **Nhãn (VI/EN) đọc từ cột `Label1`/`Label2` của `spec-register-map.csv`** (không còn ở `spec-Default.csv` — đã chuyển hẳn, xem docs/session-log/history.md), khóa theo `ParamKey` (`PARAM_HIGH_DURATION_MS`/`PARAM_LOW_DURATION_MS`/`PARAM_BUTTON_HOLD_MS`/`PARAM_DELAY_TIMER_1..10`). `PersistAndPushParametersAsync` ghi mỗi dòng xuống đúng `ParamKey` qua `WriteParamWordAsync` trong 1 vòng lặp duy nhất (thay vì gọi tay từng tham số). Cột giữa bọc `ScrollViewer` (`MaxHeight="620"`, cùng pattern cột phải) vì 13 dòng có thể vượt khung canvas cố định; `RowHeight="33"` (giảm từ 40, theo yêu cầu người dùng — vừa đủ hiện cả 13 dòng không cần cuộn ở độ phân giải thiết kế).
  - **10 "Delay timer" (`PARAM_DELAY_TIMER_1..10`, `D150-D159`) mặc định `5` (x0.1s), không phải `0`** — `TestParameters.DelayTimersMs` field initializer = `Enumerable.Repeat(5, 10).ToArray()`, cùng lý do "giá trị 0 gây mơ hồ" (Phần I mục 3). Áp dụng cho cả (1) khởi tạo lần đầu (chưa có `test-parameters.json`), (2) file config cũ lưu `0` — `SettingTabViewModel.LoadAsync` coi `0` là "chưa gán" và tự nạp lại `5` khi hiển thị (tự "chữa lành" file JSON ngay lần lưu kế tiếp). Đánh đổi: không còn cách nào để 1 trong 10 ô này thật sự bằng `0`. **LƯU Ý**: file `test-parameters.json` cũ (lưu từ trước khi đổi đơn vị) giữ nguyên `HighModeDurationMs=2000`... — KHÔNG tự chuyển đổi, cần xóa `test-parameters.json` để nhận mặc định mới.
  - **`LoadParametersAsync()` tự đẩy TOÀN BỘ tham số xuống PLC ngay sau khi nạp xong (`PersistAndPushParametersSafeAsync()`), không chờ người dùng sửa gì** — sửa theo yêu cầu người dùng: các thanh ghi `PARAM_*` (bao gồm 13 dòng Timer Setting, `ScanCodeLength`, `AllowEditBarcode`, `RequireScanOrder`, `TestOrderLowToHigh`, `ScanRevMode`) đều **chỉ-ghi, PC không đọc ngược lại để xác nhận** — nếu chỉ hiển thị lên UI mà không chủ động đẩy, PLC có thể vẫn giữ giá trị cũ/0 (VD sau khi PLC bị mất điện/reset độc lập với PC) cho tới lần "Xác nhận thay đổi"/sửa tham số kế tiếp, gây lệch giữa UI và PLC mà người vận hành không biết. Trước đây chỉ đẩy khi có 1 cascade `OnXxxChanged`/`ValueCommitted` xảy ra (sửa tay hoặc bấm Xác nhận thay đổi) — giờ luôn đẩy 1 lần ngay lúc khởi động app (`App.xaml.cs` gọi `LoadParametersAsync()` đúng 1 lần), quan trọng nhất với các giá trị `DelayTimersMs` vừa được "chữa" từ `0` → `500` ở trên. Ghi cache-first như mọi lệnh ghi PLC khác (mục 6/7 Tab 4) — nếu app khởi động trước khi driver kết nối xong (Role=Master) thì chỉ cập nhật cache, tick polling đầu tiên sau khi kết nối xong sẽ tự đẩy vật lý qua `WriteRangeFromCacheAsync(100, 100, ...)` (Phase 2, D100-D199, mỗi ~500ms).
    - **BUG THẬT đã gặp + đã sửa ngay trong cùng phiên: gọi `await PersistAndPushParametersSafeAsync()` (thay vì fire-and-forget) làm app "trông như không khởi động được"** — hàm này ghi TUẦN TỰ 18 thanh ghi PLC (5 tham số + 13 dòng Timer Setting); mỗi lệnh ghi khi PLC không phản hồi tự retry qua `Transport.Retries = RetryCount` (mặc định 3) × `TimeoutMs` (mặc định 1000ms, xem `ModbusRtuDriver`) — tối đa ~4s/lệnh, tổng cộng **tới ~72 giây**. `LoadParametersAsync()` được `App.xaml.cs` await TRƯỚC `mainWindow.Show()`, nên trên máy không có PLC thật trả lời đúng (COM sai/PLC chưa đấu/chưa cấp điện), app phải "chờ" gần 1 phút rưỡi mới hiện cửa sổ — verify bằng cách đo trực tiếp (poll `EnumWindows`/`IsWindowVisible` mỗi 0.5s): bản lỗi không tìm thấy cửa sổ nào dù đã đợi 45s+; sau khi đổi `await` → `_ = PersistAndPushParametersSafeAsync()` (fire-and-forget, cùng pattern mọi nơi khác trong ShellViewModel/SettingTabViewModel), cửa sổ hiện ngay lập tức (0s ở lần poll đầu tiên). Bài học: bất kỳ vòng lặp ghi PLC hàng loạt nào cũng KHÔNG được `await` trực tiếp trong đường khởi động app trước `MainWindow.Show()` — luôn fire-and-forget để 1 PLC chậm/không phản hồi không chặn cả UI.
- Chế độ kiểm tra: "Cho phép sửa Barcode" (xem mục 7 Tab 1 "Scan Barcode").
- Cấu hình tách số serial theo vị trí ký tự bắt đầu/kết thúc — giữ nguyên như bản cũ.
- **6 biến đo lường (High/Low mode × Điện áp/Dòng điện/Lực hút chân không) đọc giá trị thật qua địa chỉ thanh ghi `TestStepDefinition.Address`** — cột "Địa chỉ" (chỉ đọc) hiện ở cả 2 bảng High/Low mode trong tab Set Spec. (không hiện ở tab Main — màn hình vận hành/khách hàng xem, giữ đúng nguyên tắc "không đưa chi tiết kỹ thuật lên UI khách hàng"). Nguồn Address là **1 file CSV cố định trong source tree** (`src/EolTester.Configuration/SeedData/spec-register-map.csv`, cột `Key,Address,Scale,...`, copy ra cùng thư mục exe khi build) — **không có cơ chế Import/Export qua UI**, chỉ đọc, nạp lại mỗi lần `JsonSpecProfileStore.LoadAsync` chạy (`CsvSpecRegisterMapSource`) và ghi đè `Address`/`Scale` theo đúng `Key` khớp. Sửa địa chỉ/Gain = sửa trực tiếp file CSV này rồi build lại, không cần dò trong code C#, không cần xóa `spec-profile.json` cũ.
  - **Hệ số Gain (`Scale`) data-driven theo cột thứ 3 của CSV** — chỉ nhận `1/10/100/1000` (`EolTester.Core.PlcGainScale.IsValid`, khác/thiếu → mặc định 1 = không scale), chỉ có ý nghĩa cho 6 Key đo lường và cặp `LowerLimit`/`UpperLimit` D134-D145 đi kèm (không áp dụng cho cột CSV xuất kết quả hay các Key `CMD_`/`PARAM_`/`SIGNAL_` khác). Số chữ số thập phân hiển thị/làm tròn đi kèm 1-1 với Scale: 1→0 số lẻ, 10→1, 100→2, 1000→3 (`PlcGainScale.Round`, `MidpointRounding.AwayFromZero`). `TestStepDefinition.LowerLimit`/`UpperLimit` là `decimal?` (giá trị THỰC, đã quy đổi — không phải số nguyên thanh ghi PLC) + `Scale` (`int`, mặc định 1). `PlcPollingService.WriteStepLimitsAsync` ghi `raw = round(value*Scale)`; `ReadMeasurement`/`ReadRegisterLimit` đọc/tính `raw/Scale` rồi làm tròn theo `PlcGainScale.Round`. Cột **"Hệ số PLC (Gain)"** (resx `Main_PlcGain`, chỉ đọc, `Width="110"`) hiện ở cả 2 bảng High/Low mode trong tab Set Spec., bind thẳng `Scale`.
  - **Đây là quyết định đã đảo ngược 1 lần trong ngày**: từng chốt "truyền thô, không nhân/chia 100" (bỏ hẳn quy ước ×100 cứng ban đầu), sau đó người dùng yêu cầu đưa lại hệ số nhân/chia nhưng theo hướng **data-driven per-Key** thay vì hardcode ×100 như lúc đầu — đây là quyết định cuối cùng, hiện hành. **Hệ số Gain THẬT của từng biến đo lường chưa được xác nhận với PLC/kỹ sư phần cứng** — toàn bộ đang mặc định `1` trong `spec-register-map.csv`, cập nhật trực tiếp file này (chỉ cần build lại để copy CSV, không cần sửa code) khi có số liệu thật. Chi tiết đầy đủ xem docs/session-log/history.md mục 39.
  - Bước chưa gán Address vẫn dùng dữ liệu giả lập (Debug)/0 (Release) như trước.
- **Xuất kết quả ra file CSV theo ngày (đã cài đặt, khối "XUẤT FILE CSV KẾT QUẢ", nằm ngay dưới 2 checkbox "Cho phép sửa Barcode"/"Bắt buộc đúng thứ tự tem", yêu cầu Admin)** — **cột hoàn toàn linh hoạt** (nâng cấp từ thiết kế "3 cột cố định Date/Time/Barcode + Dxxxx tùy chỉnh" ban đầu):
  - **Cấu hình** (`CsvExportSettings`, `EolTester.Configuration.Models`, lưu `csv-export-settings.json` qua `JsonCsvExportSettingsStore`): `OutputDirectory` + `ColumnSpecs` (`List<string>`, đổi tên từ `ColumnAddresses` — mỗi phần tử giờ có thể là 1 token đặc biệt HOẶC 1 địa chỉ `Dxxxx`/`Dxxxx.b`).
  - **`OutputDirectory` rỗng KHÔNG còn nghĩa là "tắt tính năng"** (bug đã sửa — trước đây `AppendRowAsync` return sớm nếu rỗng, lặng lẽ không ghi gì) — xuất CSV là tính năng bắt buộc của giai đoạn hiện tại (mục 4), không được phép mất dữ liệu chỉ vì Admin quên cấu hình thư mục. `AppPaths.ResolveCsvExportDirectory(configured)` (`EolTester.App/Services/AppPaths.cs`) luôn trả về 1 thư mục dùng được: `configured` nếu Admin đã tự chọn, hoặc `AppPaths.DefaultCsvExportDirectory` nếu chưa — ưu tiên 1 là `<Documents của user>\EolTester\csv_export`, ưu tiên 2 (Documents không lấy được, VD tài khoản service không có profile) là `csv_export` cạnh file .exe. Dùng chung ở cả `CsvResultExportService.AppendRowAsync` (ghi thật) lẫn `SettingTabViewModel.LoadAsync` (hiển thị ô "Thư mục lưu CSV" ở Set Spec. — Admin luôn thấy đúng đường dẫn thật sự đang dùng, không bao giờ thấy ô trống trong khi CSV vẫn đang ghi ở nơi khác).
  - **`ShellViewModel` chặn ghi CSV/scanlog nếu `LastScanOk` ("Mã Scan ghi nhận") đang rỗng** (bug đã sửa — trước đây `CsvWriteSignalRaised` cứ ghi vô điều kiện, có thể tạo dòng CSV/scanlog với barcode rỗng nếu PLC bắn `SIGNAL_CSV_WRITE` trước khi có lần quét hợp lệ nào) — cả `AppendCsvRowSafeAsync` lẫn `RecordScanLogSafeAsync` (xem "Chống trùng barcode" ở trên) đều dùng đúng `LastScanOk`, KHÔNG phải `ScanBarcodeText` (buffer đang gõ dở). Rỗng → bỏ qua cả 2, log cảnh báo + `AddNotification` (`Notify_CsvWriteSkippedEmptyBarcode`).
  - **`PARAM_SCAN_CODE_READY` (`spec-register-map.csv`, `D104.2`)** — bit PC ghi xuống PLC, cách lái **khác nhau theo mode** (đổi theo yêu cầu người dùng — trước đây luôn bám `LastScanOk` và giữ `true` mãi tới khi tắt app):
    - **SCAN MODE**: gắn đúng vòng đời xung `CMD_START`. `PlcPollingService.PulseCommandAsync("CMD_START", scanCodeReadyForThisStart:)` nâng `D104.2` lên `1` **cùng lúc** `CMD_START` lên — chỉ khi `scanCodeReadyForThisStart=true` (SCAN MODE tự động luôn truyền `true`; nút Start thủ công truyền `!string.IsNullOrWhiteSpace(LastScanOk)`) — và **LUÔN** hạ `D104.2` về `0` khi xung `CMD_START` tắt (kể cả khi bit đang treo `1`). Ô "Mã Scan ghi nhận" ở footer giữ nguyên hiển thị khi bit hạ (`LastScanOk` không bị xóa theo).
    - **REV MODE**: bám theo **MỨC** "Mã Scan ghi nhận khác rỗng" — `ShellViewModel.OnLastScanOkChanged` đẩy `D104.2 = !string.IsNullOrWhiteSpace(LastScanOk)` mỗi khi `LastScanOk` đổi (REV MODE không tự phát `CMD_START`, `PulseCommandAsync` nhận `null` nên không đụng bit). Vì không có cơ chế nào đưa `LastScanOk` về rỗng trong phiên → thực tế `D104.2` lên `1` ở lần quét đầu rồi giữ `1`.
    - **Chuyển mode** (`ShellViewModel` subscribe `SettingTab.ScanRevMode`): vào REV MODE → đẩy lại `D104.2` theo mức `LastScanOk` hiện tại; vào SCAN MODE → ép `D104.2 = 0` (chờ xung `CMD_START` kế tiếp) + reset `_previousSerialNumber`.
    - **Chọn `D104.2` sau khi phát hiện xung đột thật**: đề xuất ban đầu là `D104.4`, nhưng địa chỉ đó đã dùng cho `PARAM_SCAN_REV_MODE` — `D104.2` là bit trống duy nhất giữa các `PARAM_` trong từ D104. **Chưa test với PLC thật** (giống mọi thanh ghi D104.x khác, xem mục 13).
  - **`EolTester.Core.CsvColumnSpecParser`/`CsvColumnSpec`** (pure, unit-test được — `tests/EolTester.Core.Tests/CsvColumnSpecParserTests.cs`) nhận diện 1 phần tử (sau khi tách theo dấu phẩy) thành 1 trong 5 loại: `Sequence` (`<STT>`, số thứ tự dòng **trong đúng file** đang ghi, reset khi sang file/ngày mới), `BarcodeColumn` (`<barcode>`), `JobIdColumn` (`<JOB>`, lấy `ShellViewModel.ConfirmedJobId` tại thời điểm ghi), `DateTimeColumn` (`<date>`/`<time>` = định dạng ngắn theo `CultureInfo.CurrentCulture`/Windows; hoặc composite ghép từ sub-token `<dd>/<mm>/<yyyy>/<hh>/<min>/<sec>`, VD `<yyyy>-<mm>-<dd>` → format .NET `yyyy-MM-dd`), hoặc `RegisterColumn` (mặc định nếu không khớp token nào — địa chỉ Dxxxx/Dxxxx.b như cũ).
  - **Cột Barcode LUÔN có mặt** — `CsvResultExportService.BuildEffectiveSpecs` tự chèn `BarcodeColumn` vào ĐẦU danh sách nếu Admin không tự gõ `<barcode>` (gõ tường minh thì dùng đúng vị trí đó) — đã chốt với người dùng: đây là cột duy nhất KHÔNG thuộc diện tùy chọn.
  - **Cột dữ liệu tùy chỉnh nhập trên CÙNG 1 DÒNG** (`SettingTabViewModel.CsvColumnsText`, TextBox, cách nhau bằng dấu phẩy VD `<STT>, <date>, D50`) — không dùng bảng nhiều dòng (từng bị bỏ vì tràn khung canvas cố định, xem mục 12). `ComboBox` "gợi ý" bên cạnh liệt kê 4 token đặc biệt cố định (đầu danh sách) + các Key **có khai báo Heading** trong `spec-register-map.csv` (hiển thị `"{Heading} ({Address})"`) — chọn 1 mục tự nối `InsertText` vào cuối ô nhập.
  - **`spec-register-map.csv` thêm 2 cột mới** `Heading`/`Type` (cấu trúc giờ là `Key,Heading,Type,Address,Scale,Label1,Label2` — Heading/Type chèn ngay sau Key): `Heading` = tiêu đề cột CSV (thay cho tra ngược Key như thiết kế cũ) **kiêm bộ lọc ComboBox gợi ý** (Key không có Heading → không hiện, ẩn hết `CMD_*`/`PARAM_*`/`SIGNAL_*` nội bộ); `Type` hiện chỉ có giá trị `OKNG` — khi đó giá trị ghi ra là chuỗi "OK"/"NG" thay vì số thô (word: 1→OK/2→NG; bit `Dxxxx.x`: 1→OK/0→NG), **bỏ qua Scale** (Scale chỉ có ý nghĩa với giá trị đo lường số). Đã seed Heading/Type cho 6 biến đo lường + 6 cặp OkNg tương ứng, để trống cho các Key còn lại.
  - **"Lưu cấu hình CSV" validate từng token**: token đặc biệt luôn hợp lệ; `RegisterColumn` phải đúng cú pháp `Dxxxx`/`Dxxxx.b` **và** khớp đúng 1 Key **có Heading** (siết chặt hơn quy tắc cũ "khớp Key bất kỳ") — sai thì chặn lưu, liệt kê lỗi qua `StatusMessage` dùng chung (`CsvSpecRegisterMapSource.LoadAsync` giờ tự đọc qua `ReadRowsAsync` lấy đúng cột Address theo chỉ số, KHÔNG dùng được `CsvKeyValueFileReader.ReadAsync` chung nữa vì cột 2 giờ là Heading không phải Address).
  - **Trigger ghi 1 dòng: PLC tự báo qua 1 bit riêng** — `SIGNAL_CSV_WRITE` (`spec-register-map.csv`, `D70.4`), cùng pattern rising-edge (0→1, PLC tự tắt lại) với `SIGNAL_SCAN_CONFIRM`. PC hoàn toàn bị động, không tự polling/tự đoán thời điểm "1 chu trình test đã xong" — PLC chủ động quyết định.
  - **`CsvResultExportService.ResolveTargetFileAsync`** — dò `<ngày>.csv` → nếu chưa tồn tại thì dùng (file mới, ghi header theo cấu hình hiện tại); nếu tồn tại và dòng header (đã ghép đúng theo `ColumnSpecs` hiện tại) khớp thì dùng lại; nếu khác thì thử `<ngày>-1.csv`, `-2.csv`... tăng dần tới khi gặp file chưa tồn tại hoặc header khớp — cho phép Admin bật/tắt qua lại 2 cấu hình trong ngày mà không sinh vô hạn file, **và tự động không đụng tới file CSV cũ ghi trước khi có tính năng này** (header cũ `Date,Time,Barcode,...` chắc chắn không khớp header mới, nên lần ghi đầu tiên sau nâng cấp tự tách sang file `-1.csv`, không cần migrate thủ công). Chỉ đọc dòng đầu tiên mỗi file ứng viên (không đọc cả file) nên chạy lại mỗi lần ghi, không cần cache riêng.
  - **Cache chống trùng barcode + đếm dòng cho `<STT>`** gộp chung 1 cơ chế (`_cachedFilePath`/`_cachedBarcodes`/`_cachedRowCount`) — nạp lại (đọc cả file 1 lần) khi file resolve ra khác file đã cache (đổi ngày HOẶC đổi cấu hình cột khiến resolve ra file `-n` khác), sau đó chỉ cập nhật tăng dần, không đọc lại. Vị trí cột Barcode trong file (để biết cột nào so trùng) suy từ chính `ColumnSpecs` hiện tại, không còn cố định "cột thứ 3" như thiết kế cũ.
  - **BOM UTF-8 tự viết tay 1 lần** (chỉ khi file mới) — không đổi so với thiết kế cũ.
  - **Chưa verify bằng UI thật cho vòng nâng cấp này** (khác các tính năng CSV trước — đã từng verify UI+đọc file thật) — tự động hoá đăng nhập qua synthetic keyboard input không ổn định trong phiên làm việc lúc cài đặt tính năng này, dừng ở build+unit test pass. Cần xác nhận trên bàn thử nghiệm thực tế: header đúng theo cấu hình, `<STT>` tăng đúng, giá trị OK/NG đúng quy ước, và tự tách file `-1.csv` khi đổi cấu hình giữa 2 lần ghi cùng ngày.

## 8. Bảo mật & phân quyền (áp dụng riêng cho dự án này)
- Vai trò: **3 mức** — **User → Operator → Admin** (`EolTester.Core.Enums.UserRole`, thứ tự enum tăng dần để so `>=`). Mô hình hóa bằng enum + bảng quyền, không rải rác điều kiện `if (password == "...")`. Nguyên tắc chung: **quyền cao hơn luôn bao gồm mọi tính năng của quyền thấp hơn**.
  - **Không đăng nhập (khách)**: CHỈ được **chuyển tab** + **quét barcode để vận hành máy** (SCAN MODE — quét là luồng vận hành bình thường, không bắt đăng nhập; xung `CMD_START` tự phát sau 1 lần quét hợp lệ trong SCAN MODE là 1 phần của quét nên vẫn chạy). Mọi nút điều khiển thủ công đều bị **disable**: Start/Stop/Reset, Xác nhận NG, Xác nhận Job (footer), đổi Auto/Manual + điều khiển I/O thủ công (Monitor). Đây là thay đổi so với trước (khách từng dùng đầy đủ Main+Monitor).
  - **User**: mọi quyền của khách, **cộng thêm** vận hành Main/Monitor — Start/Stop/Reset, Xác nhận NG, Xác nhận Job, đổi Auto/Manual, điều khiển I/O thủ công. KHÔNG sửa Set Spec./Setup.
  - **Operator**: mọi quyền User, **cộng thêm** chỉnh sửa thông số ở tab **Set Spec.** (giới hạn High/Low mode, `TestParameters`, "Cho phép sửa Barcode", "Bắt buộc đúng thứ tự tem", "Check Duplicate in log"). **Không** chỉnh Setup, **không** chỉnh khối "XUẤT FILE CSV KẾT QUẢ".
  - **Admin**: toàn quyền — mọi quyền Operator, cộng tab **Setup** (kết nối PLC) + khối "XUẤT FILE CSV KẾT QUẢ" + Import/Export nhãn I/O ở Monitor.
  - Cài đặt kỹ thuật: `ShellViewModel.CanSendCommands` = `IsLoggedIn && ConnectionState != Error` (footer); `MonitorTabViewModel.CanSendCommands` = `_canOperate && ...` (`RefreshPermissions(canOperate, isAdmin)`); `ShellViewModel.CanEditSpec` = `Role >= Operator` (bind `IsEnabled` grid Set Spec.); nút "XÁC NHẬN JOB" bind `IsEnabled="{Binding IsLoggedIn}"`.
- **Tài khoản đọc từ file, không hardcode** (`FileUserStore` trong `EolTester.Security`, thay `SeededUserStore` đã xóa): lần đầu (chưa có `Config\users.json`) đọc `SeedData\users-seed.csv` (`UserName,Role,Password` plaintext — điểm DUY NHẤT plaintext, chỉ trong source tree), băm PBKDF2 rồi ghi `users.json` (chỉ hash+salt). Các lần sau nạp thẳng `users.json`, bỏ qua seed. Đổi tài khoản: sửa `users-seed.csv` + xóa `users.json` + khởi động lại. Cả 2 thiếu/hỏng → bộ mặc định biên dịch sẵn (admin/admin123, operator/operator123, user/user123) để không khóa cứng app. Chưa có UI quản lý user.
- Mật khẩu lưu dạng băm + salt (PBKDF2/BCrypt), không bao giờ lưu plaintext trong config hay log.
- Mọi hành động có đặc quyền (đăng nhập/đăng xuất, đổi spec, lệnh thủ công, đổi cấu hình Setup) đều được ghi vào audit log qua `EolTester.Data`, kèm người dùng, thời điểm, và giá trị cũ/mới nếu có.
- **Tính năng "BỎ QUA LỖI" (bỏ qua phát hiện NG, từng có ở tab Set Spec.) đã bị xóa hoàn toàn theo yêu cầu người dùng** — không còn UI/ViewModel/audit log/thanh ghi PLC nào cho tính năng này (bit `D104.2` cũ nay đã tái dùng cho `PARAM_SCAN_CODE_READY`). Quy tắc chung ở Phần I mục 4 về "tính năng qua mặt kiểm tra chất lượng" vẫn giữ nguyên cho các dự án khác/tương lai — chỉ riêng dự án này hiện không còn tính năng cụ thể nào thuộc diện đó.

## 9. Cấp phép bản quyền (License gating)
- Phần mềm khóa theo máy (machine-locked) dựa trên **số serial ổ đĩa C:**, dùng đúng thuật toán hoán vị (permutation) đã có sẵn trong công cụ keygen nội bộ của người dùng (`docs/KeygenApp` — không phải công cụ bẻ khóa phần mềm bên thứ ba như hiểu lầm ban đầu, mà là công cụ cấp phép riêng cho các sản phẩm tự phát triển). Thuật toán được port nguyên vẹn (không đổi `HmIdx`/thứ tự phép toán) sang `EolTester.Security/Licensing/LicenseKeyAlgorithm.cs` để một công cụ keygen duy nhất (`docs/KeygenApp`, tab "Công cụ tạo Key") có thể tính đúng Key cho Registration Code sinh ra từ HV356 EOL Tester — không cần viết công cụ keygen riêng cho từng sản phẩm.
- Luồng hoạt động (`App.xaml.cs` → `ILicenseService.CheckLicense()`, chạy **trước** khi host start các service nền và trước khi `MainWindow` được tạo):
  1. Đọc số serial ổ đĩa C:, sinh **Registration Code** (nếu chưa có hoặc mã cũ không khớp máy hiện tại) — lưu tại `HKEY_CURRENT_USER\SOFTWARE\TTI\EolTester705715\License` (`RegistryLicenseStore`), tách biệt khỏi nhánh registry của công cụ keygen.
  2. So khớp **Registration Key** đã lưu (nếu có) với Key tính được từ Code hiện tại — khớp thì `IsLicensed = true`, cho vào thẳng `MainWindow`.
  3. Không khớp/chưa có → mở `LicenseWindow` (modal, `ShowDialog()`) hiển thị Registration Code để người dùng gửi cho nhà cung cấp, có ô nhập Key để dán mã nhận được và nút "Đăng ký". Nhập đúng Key → lưu registry, đóng dialog, tiếp tục khởi động app bình thường. Bấm "Thoát" hoặc đóng cửa sổ mà chưa đăng ký thành công → gọi `Application.Shutdown()`, không tạo `MainWindow`.
- **Lưu ý kỹ thuật WPF quan trọng**: `Application.ShutdownMode` mặc định là `OnLastWindowClose` — nếu không đổi, việc đóng `LicenseWindow` (kể cả khi đăng ký thành công) sẽ bị WPF hiểu nhầm là "cửa sổ cuối cùng đã đóng" (vì `MainWindow` chưa `Show()` tại thời điểm đó) và tự tắt ứng dụng. Đã set `ShutdownMode = ShutdownMode.OnExplicitShutdown` ngay đầu `OnStartup`, và tự gọi `Shutdown()` tường minh ở 2 chỗ: khi người dùng thoát từ `LicenseWindow` mà chưa đăng ký, và khi `MainWindow.Closed` (hook thủ công) — đây là bẫy dễ gặp khi chèn một dialog bắt buộc trước `MainWindow` trong bất kỳ ứng dụng WPF nào, không riêng tính năng license.
- **Bẫy DI cần tránh**: `LicenseWindow` **không** được đăng ký qua `services.AddTransient<LicenseWindow>()` rồi resolve bằng `GetRequiredService<LicenseWindow>()` — vì `LicenseViewModel` cũng là Transient, DI sẽ tạo ra **2 instance khác nhau** (một instance được set `RegistrationCode` ở `OnStartup`, một instance rỗng do constructor của `LicenseWindow` tự yêu cầu tiêm), khiến ô Registration Code hiển thị trống. Cách đúng: resolve `LicenseViewModel` một lần, gọi `LoadCurrentCode(...)`, rồi `new LicenseWindow(licenseViewModel)` trực tiếp — không qua DI cho riêng `LicenseWindow`.
- **Còn nợ / câu hỏi mở**: chưa có cơ chế license theo thời hạn (trial/hết hạn) — hiện chỉ là khóa vĩnh viễn theo máy; chưa có màn hình "Giới thiệu/About" ở tab Setup hiển thị trạng thái đăng ký hiện tại (đang chỉ thấy lúc khởi động); chưa quyết định máy thay ổ cứng C: hoặc cài lại Windows thì xử lý cấp lại license thế nào (hiện tại: Registration Code sẽ đổi theo serial ổ đĩa mới, cần key mới từ nhà cung cấp).
- **Chặn mở app 2 lần cùng lúc (single-instance)** — dùng named `Mutex` Windows (`App.xaml.cs`, `SingleInstanceMutexName`), kiểm tra **ngay đầu** `OnStartup` (trước cả license check): `new Mutex(initiallyOwned: true, name: ..., createdNew: out var isFirstInstance)`. Không phải instance đầu tiên → hiện `SingleInstanceWarningWindow` (dialog cảnh báo song ngữ VI/EN, `ShowInTaskbar="False" Topmost="True"`) rồi `Shutdown()` ngay, không tạo `MainWindow`/không chạm license/không mở cổng COM — tránh 2 tiến trình cùng tranh chấp 1 cổng COM Modbus (hậu quả nếu không chặn: 1 trong 2 luôn nhận `IOException`/`UnauthorizedAccessException`, hoặc tệ hơn là dữ liệu đọc/ghi xen kẽ sai lệch khó debug). `OnExit` giải phóng + dispose mutex trong try/catch (`ApplicationException` nếu thread hiện tại không sở hữu mutex — trường hợp instance thứ 2 gọi `OnExit` mà chưa từng `initiallyOwned` thành công).

## 10. Đa ngôn ngữ runtime (VI/EN)
- **2 nút cờ** (`Assets/VI.png`, `Assets/EN.png` — copy từ `docs/logo/`) đặt ở cột ngoài cùng bên phải của header dùng chung trong `MainWindow.xaml`, bấm vào đổi **toàn bộ chữ hiển thị trên mọi tab đang mở ngay lập tức, không cần khởi động lại**. Đây là hạ tầng dùng chung bắt buộc cho **mọi màn hình hiện tại và sau này** — không được hardcode chuỗi tiếng Việt trong XAML/code-behind nữa.
- **Cơ chế kỹ thuật**:
  - `Translation` (singleton, `EolTester.App/Localization/Translation.cs`) — bọc `System.Resources.ResourceManager`, tra cứu chuỗi bằng `CultureInfo` **tường minh mỗi lần** (không dựa vào `Thread.CurrentUICulture`, để đổi ngôn ngữ có hiệu lực ngay không cần restart). Expose indexer `this[string key]`; khi đổi `CurrentCulture`, raise `PropertyChanged(this, new PropertyChangedEventArgs("Item[]"))` — đúng quy ước WPF cho indexer binding, mọi binding `Path=[Key]` tự refresh.
  - `TrExtension` (`MarkupExtension`, namespace `loc`) — cú pháp `Text="{loc:Tr Header_Title}"`, bên trong trả về một `Binding` trỏ `Translation.Instance[key]`. Dùng cho **mọi** chuỗi tĩnh trong XAML thay vì viết literal.
  - `ILanguageService`/`LanguageService` (`EolTester.App/Services/`) — sở hữu `CurrentLanguageIndex` (0 = vi-VN, 1 = en-US, **2 để dành cho ngôn ngữ thứ 3 sau này**), đẩy culture vào `Translation.Instance`, raise `LanguageChanged`, và lưu lựa chọn qua `ILanguagePreferenceStore`/`JsonLanguagePreferenceStore` (`language.json` trong `AppPaths.ConfigDirectory` — rập khuôn đúng pattern `JsonConnectionSettingsStore` đã có). `languageService.InitializeAsync()` chạy **ngay sau khi build xong DI container, trước cả bước kiểm tra license** trong `App.xaml.cs`, để `LicenseWindow` cũng hiện đúng ngôn ngữ đã lưu từ lần chạy trước.
  - Resx: `Resources/Strings.resx` (mặc định/invariant = tiếng Việt) + `Resources/Strings.en-US.resx` (tiếng Anh, MSBuild tự build satellite assembly theo hậu tố culture, không cần sửa `.csproj`). Tra bằng `ResourceManager.GetString(key, culture)` theo key dạng chuỗi — không dùng Designer class sinh tự động.
  - **3 converter chỉ tạo chữ tĩnh từ giá trị bind** (`RunningToTextConverter`, `ConnectionStateToTextConverter`, `BoolToTextConverter`) **đã bị xóa** — lý do: converter chỉ re-run khi giá trị đầu vào đổi, không phải khi đổi ngôn ngữ, nên chữ sẽ "đứng hình" sai ngôn ngữ sau khi bấm cờ. Thay bằng property tính toán trong ViewModel (theo đúng pattern đã có sẵn ở `ShellViewModel.StartButtonLabel`/`OnIsRunningChanged`: property get-only + tự gọi `OnPropertyChanged`), và ViewModel subscribe `ILanguageService.LanguageChanged` để re-raise khi đổi ngôn ngữ (ví dụ `ShellViewModel.ConnectionStateText`, `SettingTabViewModel.SkipNgStatusText`).
  - Các placeholder tĩnh gắn trên converter dùng lại được (ví dụ `NullableNumberToTextConverter` cho "chưa cấu hình") **không thể** nhận `{loc:Tr}` trực tiếp vì `Placeholder` là property CLR thường, không phải `DependencyProperty` — converter tự tra `Translation.Instance[PlaceholderKey]` bên trong `Convert()` thay vì nhận chuỗi cố định qua XAML.
  - Chuỗi thông báo động có tham số (vd `AddNotification($"Đăng nhập: {user}")`) chuyển thành resx key có `{0}` (`Notify_Login = "Đăng nhập: {0}"`), gọi qua `string.Format(Translation.Instance["Notify_Login"], user)`.
- **Còn nợ**: audit log (SQLite) vẫn ghi mô tả hành động bằng tiếng Việt cố định (không đi qua resx) — chấp nhận được vì đây là bản ghi lịch sử nội bộ, không phải chữ hiển thị runtime cho người vận hành; nếu sau này cần audit đa ngôn ngữ thì đổi sang lưu action code + format lúc hiển thị.

## 11. Tab Monitor — I/O linh hoạt + Import/Export nhãn qua CSV
- **Bỏ giới hạn cứng 16 Input + 16 Output**: `IoPointDefinition` đổi từ 1 trường `Label` thành **`Label1`** (bắt buộc), **`Label2`**, **`Label3`** (nullable, dự phòng ngôn ngữ thứ 3) + hàm `GetLabel(int languageIndex)` (rơi về `Label1` nếu slot tương ứng trống) — dùng chung cho cả hiển thị UI lẫn Export CSV. `IoPointRowViewModel.DisplayLabel` (thay cho `Label` cũ, vốn không tự refresh) tính từ `Definition.GetLabel(ILanguageService.CurrentLanguageIndex)`, subscribe `LanguageChanged` để tự cập nhật khi đổi cờ ngôn ngữ.
- **Giao diện nhiều cột**: mỗi hướng (Input/Output) hiển thị tối đa **16 dòng/cột**, tự động wrap sang cột kế tiếp khi vượt 16 điểm — `MonitorTabViewModel.InputColumns`/`OutputColumns` (`ObservableCollection<ObservableCollection<IoPointRowViewModel>>`, chia bằng `.Chunk(16)`, rebuild khi `Inputs`/`Outputs` đổi qua `CollectionChanged`), bind vào `ItemsControl` (panel `StackPanel Orientation="Horizontal"`) lặp qua từng cột, mỗi cột là 1 `DataGrid` con dùng chung `DataTemplate`. Bỏ hẳn cột "Giá trị" (Value) — chỉ còn Điểm + Nhãn (Input) và Điểm + Nhãn + Điều khiển thủ công (Output); trạng thái ON dựa hoàn toàn vào màu xanh trên ô "Điểm" (đã có sẵn từ trước). **Bẫy XAML**: `DataGridColumn.Width` là `DataGridLength`, không nhận `StaticResource` kiểu `double` (ném `XamlParseException`/`ArgumentException` lúc runtime, không phải lúc build) — phải viết literal số trực tiếp trong `Width="80"`; vì các cột đã nằm trong 1 `DataTemplate` dùng chung cho mọi cột nên vẫn đảm bảo đồng bộ mà không cần resource riêng.
- **Input và Output nằm chung 1 `ScrollViewer` ngang duy nhất** (mỗi bên là 1 `StackPanel Orientation="Vertical"` gồm tiêu đề + `ItemsControl`, đặt cạnh nhau trong 1 `StackPanel Orientation="Horizontal"` ngoài cùng), **không** chia 2 vùng `ColumnDefinition Width="*"` riêng biệt (50/50) cho Input/Output. **Bẫy đã gặp**: chia cứng 50/50 khiến bên Output (cần bề rộng lớn hơn Input vì có thêm cột "Điều khiển thủ công") bị thiếu chỗ, phải tự cuộn ngang bên trong vùng của nó — và thanh cuộn ngang đó chiếm mất một phần chiều cao, khiến dòng cuối cùng (dòng 16) bị che dù `RowHeight` vẫn đủ. Dịch layer bằng `RenderTransform`/`Margin` âm chỉ che được phần nhìn thấy (và margin âm còn bị Grid tự clip theo biên cột — xem bẫy dưới), không giải quyết được gốc rễ thiếu bề rộng; phải gộp về 1 dòng cuộn chung, để mỗi bên tự lấy đúng bề rộng theo nội dung, chỉ cuộn thật khi tổng bề rộng thực sự vượt khung nhìn (ví dụ sau khi Import CSV thêm nhiều cột).
- Khoảng cách giữa khối Input và khối Output bằng đúng khoảng cách giữa 2 cột trong cùng 1 khối (`Margin="0,0,10,0"` cho cả 2 — đồng bộ, không còn khoảng hở lớn bất thường). Độ rộng cột Nhãn (Input `223`, Output `193`) được cân chỉnh để tổng bề rộng 4 cột (2 Input + 2 Output mặc định) vừa khít trong canvas 1400, không phát sinh thanh cuộn ngang ở trạng thái mặc định — nếu số điểm/cột tăng thêm qua Import CSV, thanh cuộn ngang sẽ tự xuất hiện lại (đây là hành vi đúng, không phải lỗi). Cả 2 `DataGrid` template đặt `CanUserResizeColumns="False"` — khóa hẳn thao tác kéo giãn cột bằng chuột để giữ đúng tỉ lệ bố cục đã cân chỉnh, tránh người vận hành vô tình làm lệch giao diện.
  - **Bài học quan trọng khi "co hẹp tối thiểu để vừa khít"**: công thức tính tay (cộng trừ margin/padding lý thuyết) **luôn sai lệch so với thực tế** vì `DataGrid` có chi phí viền/chrome nội bộ không thể tính trước chính xác (viền `BorderThickness` mặc định, bo góc, làm tròn khi scale qua `Viewbox`). Cách làm đúng là **dò từng bước nhỏ và đo lại bằng pixel thật sau mỗi lần build** (chụp `PrintWindow`, crop vùng mép phải, so khoảng trống còn lại) — không suy luận 1 lần rồi dừng. Ví dụ thực tế: 220/190 (dư ~45px, hơi nhiều) → 227/197 (thanh cuộn quay lại, quá tay) → 223/193 (dư ~18px, đạt). Không có cách tính tắt đáng tin cậy hơn việc đo trực tiếp cho loại thay đổi "vừa khít pixel" này.
- **Bẫy thứ 2**: dùng `Margin` âm để "dịch" một phần tử sang trái ra ngoài biên cột `Grid` của nó **không có tác dụng** — `Grid` tự động cắt (clip) phần tử theo đúng biên ô đã cấp phát bất kể `Margin` âm. Phải dùng `RenderTransform` (`TranslateTransform`) nếu chỉ cần dịch hình ảnh thuần túy (không đổi layout), nhưng cách đúng đắn hơn cho trường hợp "thiếu bề rộng" là sửa lại cấu trúc layout (xem bẫy trên) thay vì dịch hình ảnh.
- **Import/Export nhãn qua CSV** (nút "IMPORT LABEL"/"EXPORT LABEL" ở dải trên cùng tab Monitor, **yêu cầu quyền Admin** — tái dùng pattern `IsAdmin`/`RefreshPermissions` sẵn có):
  - **1 file CSV chung** cho cả Input lẫn Output — 5 cột: `Address` (địa chỉ bit), `Key` (Text mục điểm, cũng dùng suy ra hướng I/O qua tiền tố `X`=Input/`Y`=Output — **không có cột Direction riêng**), `Label1` (bắt buộc), `Label2`, `Label3` (tùy chọn, rơi về `Label1` nếu thiếu).
  - `IIoLabelCsvService`/`CsvIoLabelService` (`EolTester.Configuration`, song song `JsonIoMapStore`) — tự viết reader/writer RFC4180 cơ bản (không thêm thư viện ngoài như CsvHelper, đúng tinh thần không trừu tượng hóa vượt nhu cầu), UTF-8 có BOM để Excel hiển thị đúng dấu tiếng Việt.
  - **Import = thay thế toàn bộ (Replace)** danh sách điểm — đây chính là cách người dùng tự định nghĩa số lượng/địa chỉ tín hiệu, không còn bị giới hạn bởi seed cứng 16+16 trong code. Lỗi dòng phân biệt 2 mức: **Error** (thiếu Address/Key/Label1, hoặc Key không bắt đầu bằng X/Y) → **chặn toàn bộ import**, không ghi đè `io-map.json`, hiện danh sách lỗi theo dòng cho người dùng; **Warning** (thiếu Label2/Label3) → vẫn cho qua vì đã có fallback hiển thị.
  - Import thành công → `IIoMapStore.SaveAsync(...)` ghi đè `io-map.json` → `PlcPollingService.ReloadIoMapAsync()` (method mới: load lại profile, `Clear()` rồi build lại `Inputs`/`Outputs` **tại chỗ** thay vì thay instance collection, vì `MonitorTabViewModel` đang trỏ thẳng vào cùng 2 collection này) → **có hiệu lực ngay, không cần khởi động lại app**. `PollLoopAsync` được bọc thêm `catch (InvalidOperationException)` để bỏ qua 1 tick polling nếu trùng thời điểm `Inputs` đang bị `Clear()`/`Add()` lại (an toàn hơn khóa đồng bộ phức tạp, chấp nhận được vì polling mỗi 500ms).
  - Export mặc định gợi ý lưu tại `AppPaths.IoLabelExportDirectory` = thư mục **`DATA` nằm cạnh file .exe đang chạy** (`AppContext.BaseDirectory`, **khác** `AppPaths.LogsDirectory` chứa audit-log DB — xem mục 12b). Dùng `Microsoft.Win32.OpenFileDialog`/`SaveFileDialog` có sẵn trong WPF, không cần thêm package.
  - Cả 2 thao tác đều ghi audit log qua `IAuditLogService` như các hành động đặc quyền khác.
  - Thông báo kết quả Import/Export (`MonitorTabViewModel.StatusMessage`) hiển thị ở **2 nơi cùng lúc**: ngắn gọn ngay bên trái 2 nút IMPORT/EXPORT LABEL (phản hồi tức thì tại chỗ thao tác), và đồng thời được `ShellViewModel` bơm thêm 1 dòng vào **"Notification / error history"** dùng chung (đã hiển thị ở tab Main) qua việc subscribe `MonitorTab.PropertyChanged` — để không mất thông báo khi operator đã chuyển sang tab khác. Kèm `MonitorTabViewModel.StatusMessageIsError` (bool) — thông báo lỗi tô đỏ, thành công tô xanh (trước 2026-08-28 tô cứng `DarkRed` nên "Import thành công N điểm" cũng đỏ như lỗi); cùng pattern `StatusMessage`/`StatusMessageIsError` ở `SetupTabViewModel`/`SettingTabViewModel`.

## 12. Layout cố định theo độ phân giải chuẩn 1400×765 + scale đồng bộ khi resize
Toàn bộ giao diện **không còn responsive tự do** (Grid/DockPanel tính toán lại theo kích thước cửa sổ thực tế) — chuyển sang mô hình **fixed-canvas + scale đồng bộ**, để phóng to/thu nhỏ cửa sổ chỉ đổi kích thước hiển thị (chữ, nút, khoảng cách cùng to/nhỏ theo đúng 1 tỉ lệ) mà không bao giờ sắp xếp lại layout. Đây là quy tắc bắt buộc cho **mọi cửa sổ/dialog mới sau này** trong dự án, không chỉ `MainWindow`.
- **Cơ chế**: mỗi `Window` bọc toàn bộ nội dung gốc trong `<Viewbox Stretch="Fill">`, bên trong là 1 `Grid` kích thước **cố định tuyệt đối** bằng pixel thiết kế (`MainWindow`: `1400×765`; dialog nhỏ như `LicenseWindow`: dùng đúng kích thước hiện có của nó, VD `520×360`, làm canvas riêng). Nội dung/Style/cấu trúc Grid con bên trong giữ nguyên như thiết kế responsive cũ — chỉ thêm 1 lớp bọc ngoài cùng. Vì canvas luôn cố định pixel, layout bên trong không bao giờ tính lại theo kích thước cửa sổ thật → không thể vỡ layout; `Viewbox` chỉ áp `ScaleTransform` lên toàn bộ visual (kể cả chữ) cùng lúc.
  - Dùng `Stretch="Fill"` (không phải `Uniform`) — khi tỉ lệ cửa sổ đúng bằng tỉ lệ canvas (trường hợp bình thường, do bị khóa ở bước dưới) thì `Fill`/`Uniform` cho kết quả giống hệt nhau; còn khi **Maximize** mà tỉ lệ màn hình thực tế lệch khỏi tỉ lệ thiết kế (VD màn 16:9 thông thường lệch nhẹ so với 1400:765), `Fill` lấp đầy toàn bộ vùng làm việc (chấp nhận méo nhẹ) thay vì để viền trống — đã chốt với người dùng theo hướng này, không phải mặc định kỹ thuật.
- **Khóa tỉ lệ khi kéo viền cửa sổ bằng chuột**: `src/EolTester.App/Interop/WindowAspectRatioLock.cs` — `WindowAspectRatioLock.Attach(window, aspectRatio)` hook `WM_SIZING` qua `HwndSource.AddHook` (subscribe ở `Window.SourceInitialized`), chỉnh lại `RECT` đề xuất theo đúng tỉ lệ dựa vào cạnh đang kéo (`wParam` = `WMSZ_LEFT/RIGHT/TOP/BOTTOM/TOPLEFT/...`). Đây là kỹ thuật Win32 chuẩn, **không** can thiệp Maximize (Windows không gửi `WM_SIZING` khi Maximize) nên không xung đột với hành vi lấp đầy màn hình ở trên. Gọi 1 dòng trong constructor code-behind của Window (thuần UI/interop, không phải business logic).
- **Kích thước khởi tạo `MainWindow` tính động theo màn hình thực tế** (không hardcode `1400×765` làm kích thước cửa sổ, chỉ dùng làm canvas thiết kế bên trong `Viewbox`): lấy `SystemParameters.WorkArea`, `width = WorkArea.Width × 0.8` (tối thiểu `1280`), `height = width × (765/1400)` — gán trong constructor `MainWindow.xaml.cs` sau `InitializeComponent()`. XAML chỉ khai báo `MinWidth="1280" MinHeight="699"` (`= round(1280 × 765/1400)`), không khai báo `Height`/`Width` cố định.
- **Không cần sửa gì bên trong các Tab View** (`MainTabView`, `MonitorTabView`, `SetupTabView`, `SettingTabView`) — chúng nằm trong ô đã có kích thước cố định (do canvas cha `1400×765` cố định), layout tính một lần theo canvas đó rồi được `Viewbox` cha scale theo.

## 12b. Vị trí file cấu hình/log + persistence trạng thái runtime

### Thư mục cấu hình/log — cạnh exe, tự fallback
`AppPaths.RootDirectory` (`EolTester.App/Services/AppPaths.cs`) tính **1 lần lúc nạp class**: ưu tiên `<thư mục exe>\AppData` (probe ghi thử 1 file tạm); nếu KHÔNG ghi được (VD cài trong `C:\Program Files`, UAC chặn) thì rơi về `%LocalAppData%\EolTester` như trước. `AppPaths.UsingExeRelativeRoot` (bool) + 1 dòng log lúc khởi động cho biết đang dùng vị trí nào. Bên trong root có đúng 2 thư mục:
- **`Config\`** — mọi JSON đọc/sửa tay: `connection.json`, `io-map.json`, `spec-profile.json`, `test-parameters.json`, `language.json`, `register-watch.json`, `csv-export-settings.json`, **`users.json`** (mục 8), **`runtime-state.json`** (dưới), `Specs\<model>.json`.
- **`Logs\`** — `audit-log.db` (SQLite) + `eoltester-<yyyyMMdd>.log` (Serilog). (Đổi từ `%LocalAppData%\EolTester\{Config,Data,Logs}` cũ — không migrate file cũ, chạy máy mới sẽ tự seed lại từ CSV/mặc định.)
- Không đổi: `scanlog\` và `DATA\` (Import/Export nhãn I/O) vẫn cạnh exe như trước; `csv_export` mặc định vẫn ở Documents.

### `runtime-state.json` — nhớ giá trị gần nhất qua lần khởi động lại (tính năng an toàn)
`IRuntimeStateStore`/`JsonRuntimeStateStore` (`EolTester.Configuration`), model `RuntimeState { LastScanOk, PreviousSerialNumber, Registers: Dictionary<"Dxxx",int> }`. Mục đích: app bị tắt KHÔNG đúng quy trình (kill process/mất điện) vẫn khôi phục được:
- **`LastScanOk` + `PreviousSerialNumber`** — không có store nào khác giữ, cần cho check trùng/thứ tự ở SCAN MODE.
- **Ảnh chụp thanh ghi D100–D199** (BỎ các khóa `CMD_` — lệnh xung, không nên "nhớ") — lưới an toàn cho thanh ghi D1xx chưa gắn store riêng. Hiện các thanh ghi D1xx đều đã có store chuyên biệt (`test-parameters.json`/`spec-profile.json`) tự đẩy lại lúc khởi động, nên phần này chủ yếu dự phòng tương lai.
- **Ghi**: `ShellViewModel.ScheduleRuntimeStateSave()` debounce ~1s, kích hoạt khi `LastScanOk` đổi, `PreviousSerialNumber` đổi (chuyển vào SCAN MODE / commit 1 lần quét), hoặc `PlcPollingService.PersistableStateChanged` (raise cuối `WriteParamBitAsync`/`WriteParamWordAsync`/`WriteStepLimitsAsync`). Tắt trong 1s cuối chấp nhận mất (đã thống nhất).
- **Nạp**: `ShellViewModel.RestoreRuntimeStateAsync()` gọi ở `App.xaml.cs` **TRƯỚC** `settingTab.LoadParametersAsync()`/`setupTab.LoadAsync()` — để store chuyên biệt (nếu có) vẫn thắng cho phần chúng quản lý; đặt `LastScanOk` (KHÔNG còn kéo theo push `PARAM_SCAN_CODE_READY` — bit đó giờ chỉ theo `CMD_START`) + `_previousSerialNumber` + `PlcPollingService.RestorePersistableD1xxRegisters(...)` (chỉ ghi cache — vòng polling Phase 2 tự đẩy D100-D199 xuống PLC).

## 12c. Khôi phục file seed thiếu (`SeedFiles`)
4 file trong `SeedData\` (`spec-register-map.csv`, `io-map-seed.csv`, `spec-Default.csv`, `users-seed.csv`) vừa **copy ra cạnh exe** (con người sửa tay), vừa **nhúng vào `EolTester.Configuration.dll`** (`<EmbeddedResource>` + `<CopyToOutputDirectory>` trên cùng 1 item — MSBuild làm cả 2 việc) làm bản gốc khôi phục.
- **Lúc khởi động** (`App.EnsureSeedFiles()`, chạy ngay sau khi dựng logger, TRƯỚC `Host.CreateDefaultBuilder`): `SeedFiles.FindMissing(AppPaths.SeedDataDirectory)` — thiếu file nào → `MessageBox` OK/Cancel song ngữ. **Cancel/Thoát** → `Shutdown()`. **OK/Đồng ý** → `SeedFiles.RestoreMissing(...)` ghi lại byte gốc (kể cả BOM) ra `SeedData\`; file nào không ghi được (thư mục chỉ-đọc) → `MessageBox` thông báo, app vẫn chạy.
- **Lúc runtime**: `CsvKeyValueFileReader.ReadRowsAsync` và `CsvIoMapSeedSource.LoadAsync` — nếu file đĩa vẫn vắng (ghi lại thất bại) thì đọc THẲNG từ resource nhúng qua `SeedFiles.OpenEmbedded(Path.GetFileName(path))` → app hoạt động đúng dù không có file đĩa. `FileUserStore` không cần đường này (đã có `BuiltInDefaults()`).
- Đã thay cho hành vi cũ "thiếu file → degrade âm thầm" (đặc biệt `spec-register-map.csv` thiếu làm PLC "câm" không cảnh báo).

## 13. Câu hỏi còn mở / chưa quyết định
### Chặn tầng giao tiếp PLC
- **Bảng địa chỉ Modbus RTU cơ bản đã được xác nhận đúng với PLC thật cho D1000-D1002** — người dùng tự đặt D1000=60, D1001=70, D1002=80 trên PLC thật; app đọc được D1001=70, D1002=80 (khớp tuyệt đối) và D1000=50 (lệch 10 so với giá trị người dùng báo, có thể do giá trị PLC đã đổi giữa lúc báo và lúc đọc — 2 trong 3 khớp tuyệt đối qua cùng 1 đường code chứng minh cơ chế địa chỉ/đọc word về cơ bản đúng, không phải lỗi offset hệ thống). Địa chỉ D vẫn hiển thị chỉ là số nguyên = `WordAddress` (không cộng/trừ offset nào) — đúng giả định `ModbusWordAddress` từ đầu dự án, đã kiểm chứng thật, không còn là giả định suông. Vẫn cần đối chiếu thêm các địa chỉ khác (X/Y digital, D1100+ Output) khi có dịp — chưa test hết toàn bộ bảng.
- ~~Gán `CMD_CONFIRM_NG`/`CMD_START`/`CMD_STOP`/`CMD_RESET` vào coil Modbus thật~~ — **đã xong** (docs/session-log/history.md mục 19): `PulseCommandAsync` tra `spec-register-map.csv` qua `ISpecRegisterMapSource`, ghi qua `WriteBitAsync` (Dxxxx thật, ví dụ D1100 cho CMD_START) thay vì khóa logic tạm trong `MockModbusDriver`. Địa chỉ D thật vẫn cần đối chiếu khi có bảng địa chỉ PLC — hiện đang dùng D1100-D1105 do người dùng tự gán trong CSV.
- ~~Thông số cổng COM mặc định... giá trị thật vẫn cần đối chiếu khi có PLC thật~~ — **đã có giá trị thật đầu tiên**: COM4, 19200, None, 8, 1, Slave ID 2 (PC Master) — kết nối thành công, lưu trong `connection.json` hiện hành.
- ~~`ModbusRtuDriver` thật vẫn chưa viết~~ — **đã xong** (xem mục 7 Tab 3), đã kết nối được PLC thật lần đầu.
- ~~`IPlcCommunicationDriver` chỉ đọc/ghi từng thanh ghi lẻ, mỗi tick polling ~240 request Modbus tuần tự (D0-D99 + D100-D199 + Input/Output Block), khiến hiển thị PLC→PC rất chậm dù `Task.Delay` giữa 2 tick chỉ 500ms~~ — **đã sửa (bug hiệu năng thật, đã chẩn đoán + verify bằng đọc code, không suy đoán)**: thêm `IPlcCommunicationDriver.ReadRegistersAsync`/`WriteRegistersAsync` (đọc/ghi nhiều thanh ghi liên tiếp trong 1 giao dịch — Modbus FC03/FC16, cài cho cả 3 driver: `ModbusRtuDriver` qua NModbus `ReadHoldingRegistersAsync`/`WriteMultipleRegistersAsync`, `McProtocolDriver`/`SlmpDriver` qua `IMc3EFrameCodec.EncodeReadWordRequest(device, pointCount)` vốn đã hỗ trợ multi-point sẵn, chỉ chưa được dùng). `PlcPollingService.RefreshRuntimeStateAsync` Phase 1/2 giờ gọi `ReadRangeIntoCacheAsync`/`WriteRangeFromCacheAsync` — gộp mỗi dải (D0-D99, D100-D199, Input Block, Output Block) thành 1 request khối (tự chia nhỏ theo `MaxReadChunk=125`/`MaxWriteChunk=123` nếu Block cấu hình lớn hơn giới hạn Modbus), thay vì tối đa 240 request đơn lẻ mỗi tick. Vẫn giữ đúng ngữ nghĩa an toàn cũ: nếu 1 dải lỡ chồng lấn vùng không được phép đọc/ghi (VD Input Block cấu hình cũ đè lên D100-D199 — trước khi Setup có validate D200-D2000), `ReadRangeIntoCacheAsync`/`WriteRangeFromCacheAsync` tự tách thành nhiều đoạn con liên tục cùng hợp lệ thay vì gộp bừa cả dải (không throw, không ghi nhầm vùng cấm).
  - `PlcRegisterImage.UpdateBlock` (cũ, không dùng ở đâu, ném lỗi nếu vượt phạm vi) đã **xóa hẳn**, thay bằng `StoreReadBlock` (ghi nhiều giá trị đọc được vào cache, bỏ qua im lặng địa chỉ không hợp lệ — dùng cho Phase 1) và `SnapshotBlock` (lấy nhiều giá trị liên tiếp từ cache để ghi khối xuống PLC — dùng cho Phase 2).
  - **Phát hiện thêm cùng lúc chẩn đoán**: ô "Chu kỳ polling" (`PollingIntervalMs`) ở tab Setup được lưu vào `connection.json` nhưng trước đây **không hề được `PollLoopAsync` dùng tới** — vòng lặp hardcode `Task.Delay(500, ct)`. Đã nối `PlcPollingService.SetPollingIntervalMs(...)` (kẹp tối thiểu 10ms tránh busy-loop) — gọi từ `ConnectAndPollAsync` lúc khởi động/reconnect và từ `SetupTabViewModel.SaveAsync` (có hiệu lực ngay tick kế tiếp, không cần `RestartConnectionAsync`, cùng pattern `SetInputBlockRange`/`SetOutputBlockRange`).
  - Còn lại: (a) quyết định chiến lược ghi bit lẻ theo từng thanh ghi cụ thể (xem dòng dưới), (b) xác nhận bản đồ địa chỉ D thật (xem gạch đầu dòng đầu tiên ở trên), (c) benchmark thời gian thực tế 1 tick sau khi đổi sang block I/O trên PLC thật (chưa đo — chỉ mới verify đúng qua unit test + integration test TCP loopback, xem `ModbusRtuDriverTests.ReadRegistersAsync_ReturnsMultipleValuesInOneRoundTrip`/`WriteRegistersAsync_AppliesAllValuesIntoPlcSlave`).
- **PC-Slave mode đã cài đặt khung sườn** (`ModbusSlaveService`, xem mục 6, docs/session-log/history.md mục 15) nhưng còn nhiều điểm chờ PLC/hệ thống thật xác nhận: bảng thanh ghi cụ thể nào PC "công khai" cho PLC đọc/ghi khi làm Slave; PLC thật có bao nhiêu thiết bị khác trên cùng bus RS-485 và baud rate dùng chung; timeout/chu kỳ hỏi thực tế của PLC (ảnh hưởng việc `ModbusSlaveService` phải phản hồi nhanh tới đâu — chưa kiểm chứng được trên phần cứng thật). Hot-swap Master↔Slave lúc runtime (không cần khởi động lại app) cũng chưa làm — quyết định phạm vi có chủ đích, xem docs/session-log/history.md mục 15.
- Chiến lược ghi bit lẻ (`D1000.1`) khi thanh ghi dùng chung với PLC (read-modify-write, có rủi ro tranh chấp) hay PC sở hữu riêng (ghi thẳng cả từ, an toàn hơn) — cần xác định theo từng thanh ghi cụ thể khi có bảng địa chỉ PLC thật, chưa quyết định chung.

### Chặn logic nghiệp vụ / mô hình dữ liệu kết quả
- Cycle time tính từ mốc nào đến mốc nào (bắt đầu scan → kết thúc, hay bắt đầu bước test đầu → kết thúc bước cuối)?
- "Phát hiện NG" tự động kích hoạt khi 1 bước fail, hay do operator xác nhận thủ công?
- Khi 1 bước test fail: dừng ngay hay chạy hết toàn bộ bước rồi mới báo tổng hợp?
- "XÁC NHẬN SẢN PHẨM LỖI" có phải dùng để xác nhận operator đã gỡ sản phẩm lỗi khỏi line trước khi cho chạy job tiếp theo không?
- ~~Cấu trúc cột dữ liệu của file CSV xuất kết quả kiểm tra~~ — **đã chốt và cài đặt xong** (xem mục 7 Tab 4 "Xuất kết quả ra file CSV theo ngày"): 3 cột cố định Date/Time/Barcode + cột tùy chỉnh theo địa chỉ Dxxxx (Admin tự chọn qua UI), trigger ghi 1 dòng là bit PLC riêng `SIGNAL_CSV_WRITE`. Còn nợ: chưa test trên PLC thật (chỉ verify qua "Giám sát DATA" giả lập bit), và **đây KHÔNG phải cơ chế `IResultExporter`/`TestResult` như phác thảo ban đầu ở mục 4** — ghi trực tiếp giá trị thanh ghi hiện tại lúc trigger, không đi qua 1 model kết quả kiểm tra tổng hợp (Đạt/Không đạt từng bước, v.v.); nếu sau này cần model `TestResult` đầy đủ (VD để tích hợp MES) thì đây vẫn là câu hỏi mở riêng.

### Khác
- Cơ chế cấu hình thương hiệu/logo khách hàng — đã có ảnh thật (TTI + INTEK, đặt tên trung lập `Logo1`/`Logo2` không gắn cứng tên thương hiệu trong mã nguồn) nhưng đường dẫn ảnh còn hardcode trong XAML; phần mềm này chỉ dành riêng cho 705/715/INTEK hay phải hỗ trợ nhiều profile khách hàng khác nhau (ảnh hưởng việc có đáng làm cấu hình thương hiệu ngay hay không).
- Windows bản gì trên máy công nghiệp thực tế (Win10/Win11) và độ phân giải màn hình thực tế sẽ chạy (ảnh gốc khoảng 1600×850) — ảnh hưởng thiết kế layout co giãn.
- Có cần hộp thoại xác nhận trước khi Stop/Reset lúc máy đang chạy hay không (rủi ro dừng giữa chu trình do bấm nhầm).
- Tab Setup và Set spec. chưa được áp dụng phong cách giao diện mới (card "THÔNG TIN LOT" 2 cột, nút trạng thái tô màu) như Main/Monitor — cần làm tiếp để đồng bộ toàn ứng dụng.
- **Nút "XÁC NHẬN HÀNG NG" hiện đổi màu xanh dương nhạt khi giữ, không phải LimeGreen như style `ConfirmNgButton` (`Style.Trigger IsPressed`) quy định** — phát hiện qua verify UI thật sau khi đổi nút này sang dạng giữ/thả (`MomentaryButton`), nghi do chrome mặc định của `Button` tạo hiệu ứng pressed riêng đè lên trigger màu đã khai báo. Chưa sửa — chỉ là vấn đề màu sắc, chức năng gửi `CMD_CONFIRM_NG` vẫn đúng lúc nhấn/thả.
- ~~`ShellViewModel.RequireScanOrder` (checkbox "Bắt buộc đúng thứ tự tem" ở footer tab Main) KHÔNG đồng bộ với `SettingTabViewModel.RequireScanOrder` (Set Spec.)~~ — **đã xử lý**: xóa hẳn checkbox trùng lặp ở footer (thuần UI, không lưu/đẩy PLC) theo yêu cầu người dùng — chỉ còn đúng 1 nguồn duy nhất (`SettingTabViewModel.RequireScanOrder` ở tab Set Spec., có lưu file + đẩy PLC + giờ còn bị khóa/ép bởi SCAN MODE, xem mục 7 Tab 1).
- **Tài liệu hướng dẫn vận hành**: `docs/HuongDanVanHanh_705-715_v2.docx` (bản hiện hành, 2.0 — 28/08/2026; Word thật, tạo qua Word COM automation, có ảnh chụp màn hình thật (PrintWindow, đúng chuẩn Phần I mục 7) + mục lục tự động). `docs/HuongDanVanHanh_705-715.docx` (1.0) vẫn giữ lại làm bản lưu trữ, không xóa — không còn cập nhật. v2 bổ sung so với v1: mục 1.2 Cấp phép bản quyền (License), bảng phân quyền cập nhật (bỏ "bật/tắt BỎ QUA LỖI", thêm "khối XUẤT FILE CSV KẾT QUẢ"), mục 5.3 Giám sát DATA thêm ghi chú Định dạng hiển thị/Kiểu dữ liệu, tab Set Spec. viết lại hoàn toàn (6.2 Cài đặt thời gian/Timer Setting 13 dòng, 6.3 đổi "BẮT BUỘC SCAN"→"Cho phép sửa Barcode" + thêm SCAN MODE/REV MODE, 6.4 Xuất file CSV kết quả — mới hoàn toàn, xóa hẳn mục "BỎ QUA LỖI" cũ), mục 7 thêm phần SCAN MODE tự động/chống trùng mã, mục 8 thêm troubleshooting "Phần mềm chậm hiện cửa sổ lúc mới mở". Cập nhật lại tài liệu này (tạo `_v3` mới, không sửa đè `_v2`) khi UI/luồng vận hành thay đổi đáng kể tiếp theo.
  - **Bẫy đã gặp khi dựng file bằng Word COM automation**: PowerShell coi dấu ngoặc kép cong “ ” (U+201C/U+201D) TƯƠNG ĐƯƠNG dấu `"` ASCII khi tách token chuỗi (VD `"3. Tab “Trang Tự Động”"` bị hiểu thành 3 chuỗi riêng: `"3. Tab "`, `“Trang Tự Động”`, `""`) — hàm nhận 1 tham số chỉ giữ lại chuỗi ĐẦU TIÊN, các phần còn lại (kể cả nằm trong dấu ngoặc kép cong) bị **âm thầm mất trắng**, không có lỗi/cảnh báo gì khi chạy script lẫn khi mở file `.docx` ra xem qua `Content.Text`. Không liên quan tới bẫy "PowerShell `.ps1` chứa tiếng Việt cần UTF-8 BOM" đã biết (BOM vẫn đúng, dấu tiếng Việt vẫn hiển thị đúng — chỉ riêng đoạn nằm giữa cặp ngoặc kép cong bị nuốt). Phát hiện được nhờ so sánh độ dài `Content.Text` trước/sau khi sửa (15258 → 16993 ký tự) và đọc lại toàn bộ text export — **luôn phải làm bước này** trước khi báo hoàn thành, không suy đoán nội dung đúng chỉ vì script chạy không báo lỗi. Cách khắc phục: không dùng dấu ngoặc kép cong trong nội dung tài liệu tạo bằng script PowerShell (bỏ hẳn dấu ngoặc, hoặc chuyển sang cách chèn không qua `TypeText`/không đi qua tokenizer PowerShell).

## 14. Lịch sử phát triển chi tiết theo từng phiên
Toàn bộ "Tóm tắt phiên làm việc" theo từng vòng (bối cảnh, quyết định + lý do, bẫy kỹ thuật đã gặp) được lưu tại **`docs/session-log/history.md`** — **không tự nạp vào context của mọi phiên**, chỉ đọc khi cần tra cứu quyết định cũ hoặc lý do đằng sau 1 thiết kế cụ thể. Khi kết thúc 1 vòng làm việc cần ghi tóm tắt mới, **append vào file đó**, không ghi vào CLAUDE.md (xem mục 1).

