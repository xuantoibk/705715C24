<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <!-- Lõi dùng chung qua NuGet feed local (xem ..\..\nuget.config) — không còn source cục bộ trong repo này. -->
    <PackageReference Include="EolTester.Communication" Version="1.0.3" />
    <PackageReference Include="EolTester.Configuration" Version="1.0.3" />
  </ItemGroup>
</Project>
